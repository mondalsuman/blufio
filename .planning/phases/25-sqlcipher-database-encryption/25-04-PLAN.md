---
phase: 25-sqlcipher-database-encryption
plan: 04
type: execute
wave: 3
depends_on: [25-02, 25-03]
files_modified:
  - crates/blufio/src/main.rs
  - crates/blufio-storage/src/database.rs
autonomous: true
requirements: [CIPH-01, CIPH-02, CIPH-03, CIPH-04, CIPH-05, CIPH-06, CIPH-07, CIPH-08]

must_haves:
  truths:
    - "cargo test passes across entire workspace"
    - "cargo clippy reports no warnings"
    - "No raw Connection::open() in production code outside factory"
    - "All 8 CIPH requirements are satisfied"
    - "End-to-end: create DB, encrypt, backup encrypted, restore encrypted, doctor reports encryption"
  artifacts:
    - crates/blufio/src/main.rs (verified)
    - crates/blufio-storage/src/database.rs (verified)
  key_links:
    - "All CIPH-01 through CIPH-08 requirements are verifiable"
---

<objective>
Final integration verification: run full workspace tests, clippy, and verify all CIPH requirements are met end-to-end.

Purpose: Ensure all pieces work together -- encryption, centralized connections, backup/restore, doctor, and CLI -- before marking the phase complete.
Output: Clean workspace build, all tests passing, clippy clean
</objective>

<execution_context>
@/Users/suman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/suman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-sqlcipher-database-encryption/25-RESEARCH.md
@.planning/phases/25-sqlcipher-database-encryption/25-01-SUMMARY.md
@.planning/phases/25-sqlcipher-database-encryption/25-02-SUMMARY.md
@.planning/phases/25-sqlcipher-database-encryption/25-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix clippy warnings and run full workspace tests</name>
  <files>crates/blufio/src/main.rs, crates/blufio-storage/src/database.rs</files>
  <action>
Run the following commands and fix any issues:

1. `cargo clippy --workspace -- -D warnings` -- Fix all clippy warnings. Common issues to watch for:
   - Unused imports from refactoring (remove `use tokio_rusqlite::Connection` where replaced with factory)
   - Unnecessary `.map_err()` chains (open_connection already returns BlufioError)
   - Missing error handling on `.unwrap()` in non-test code
   - Redundant clones

2. `cargo test --workspace` -- Ensure all tests pass across entire workspace. Pay attention to:
   - Tests that previously opened connections directly may need env var setup
   - Tests using `open_in_memory()` should still work (no encryption on in-memory DBs)
   - Serial test ordering for env var tests

3. `cargo fmt --all --check` -- Ensure formatting is correct

4. Final verification grep -- confirm no production bypass:
   ```bash
   # Should return ZERO results (except factory itself and test code)
   grep -rn "tokio_rusqlite::Connection::open\|rusqlite::Connection::open" crates/ --include="*.rs" \
     | grep -v "database.rs" \
     | grep -v "open_in_memory" \
     | grep -v "#\[cfg(test)\]" \
     | grep -v "mod tests" \
     | grep -v "fn test_" \
     | grep -v "async fn test_"
   ```

5. If any Connection::open() calls are found in production code, replace them with the factory.

6. Verify the workspace Cargo.toml still has the correct feature:
   ```bash
   grep "bundled-sqlcipher-vendored-openssl" Cargo.toml
   ```
  </action>
  <verify>
    <automated>cd /Users/suman/projects/github/blufio && cargo clippy --workspace -- -D warnings 2>&1 | tail -5 && echo "---TESTS---" && cargo test --workspace 2>&1 | tail -10</automated>
  </verify>
  <done>
  - cargo clippy --workspace -- -D warnings passes cleanly
  - cargo test --workspace passes all tests
  - cargo fmt --all --check passes
  - No Connection::open() bypass in production code
  - bundled-sqlcipher-vendored-openssl feature confirmed in Cargo.toml
  - All CIPH-01 through CIPH-08 requirements are verifiable
  </done>
</task>

</tasks>

<verification>
CIPH requirement verification checklist:
- CIPH-01: `grep "bundled-sqlcipher-vendored-openssl" Cargo.toml` returns match
- CIPH-02: `grep "PRAGMA key" crates/blufio-storage/src/database.rs` shows key applied first
- CIPH-03: `grep "BLUFIO_DB_KEY" crates/blufio-storage/src/database.rs` shows env var sourcing
- CIPH-04: `grep "sqlite_master" crates/blufio-storage/src/database.rs` shows key verification SELECT
- CIPH-05: No raw Connection::open() in production code (grep verification)
- CIPH-06: `blufio db encrypt` command exists in CLI enum
- CIPH-07: backup.rs uses open_connection_sync() for all connections
- CIPH-08: doctor.rs has check_encryption() function
</verification>

<success_criteria>
- Zero clippy warnings
- All workspace tests pass
- All 8 CIPH requirements satisfied
- Clean, production-ready code
</success_criteria>

<output>
After completion, create `.planning/phases/25-sqlcipher-database-encryption/25-04-SUMMARY.md`
</output>
