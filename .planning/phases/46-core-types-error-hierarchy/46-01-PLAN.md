---
phase: 46-core-types-error-hierarchy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/blufio-core/Cargo.toml
  - crates/blufio-core/src/error.rs
  - crates/blufio-core/src/types.rs
  - crates/blufio-core/src/lib.rs
autonomous: true
requirements:
  - ERR-01
  - ERR-02
  - ERR-03

must_haves:
  truths:
    - "BlufioError.is_retryable() returns true for Network/RateLimit/Timeout/Unavailable failure modes and false for Auth/Validation/Internal/ResourceExhausted"
    - "BlufioError.severity() returns a Severity enum value (Fatal, Error, Warning, Info) that can be matched exhaustively"
    - "BlufioError.category() returns an ErrorCategory enum value (Provider, Channel, Storage, Security, Config, Mcp, Skill, Migration, Internal)"
    - "BlufioError.failure_mode() returns a FailureMode enum value that is_retryable() derives from"
    - "BlufioError.trips_circuit_breaker() returns true only for server-side failure modes"
    - "BlufioError.suggested_backoff() returns sensible Duration values for retryable errors"
    - "BlufioError.user_message() returns sanitized strings with no internal details"
    - "Workspace compiles with temporary fallback constructors for downstream crates"
  artifacts:
    - path: "crates/blufio-core/src/error.rs"
      provides: "BlufioError enum with 6 sub-enums, ErrorContext, classification methods, constructor helpers, error_log! macro"
      contains: "ProviderErrorKind"
    - path: "crates/blufio-core/src/types.rs"
      provides: "StreamingType, FormattingSupport, RateLimit enums/structs on ChannelCapabilities"
      contains: "StreamingType"
  key_links:
    - from: "crates/blufio-core/src/error.rs"
      to: "is_retryable"
      via: "failure_mode() derivation"
      pattern: "fn is_retryable.*failure_mode"
---

<objective>
Define the complete typed error hierarchy, classification enums, ErrorContext struct, all 6 sub-enums, 7 classification methods, constructor helpers, error_log! macro, and Prometheus counter signature in blufio-core. Extend ChannelCapabilities with 4 new typed fields. Include temporary fallback constructors so the workspace compiles.

Purpose: Foundation for all downstream migration -- every other plan depends on these types existing.
Output: Compilable blufio-core with full error type system and extended ChannelCapabilities.
</objective>

<execution_context>
@/Users/suman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/suman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-core-types-error-hierarchy/46-CONTEXT.md
@.planning/phases/46-core-types-error-hierarchy/46-RESEARCH.md
@crates/blufio-core/src/error.rs
@crates/blufio-core/src/types.rs
@crates/blufio-core/src/format.rs
@crates/blufio-core/src/lib.rs
@crates/blufio-core/Cargo.toml
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define error hierarchy types, sub-enums, ErrorContext, and classification methods</name>
  <files>crates/blufio-core/src/error.rs, crates/blufio-core/Cargo.toml, Cargo.toml</files>
  <action>
1. Add workspace dependencies to root Cargo.toml: `proptest = "1"`, `tracing-test = "0.2"`. Add dev-dependencies to blufio-core/Cargo.toml: `proptest = { workspace = true }`, `tracing-test = { workspace = true }`.

2. In error.rs, define (all with `#[non_exhaustive]`, derive Serialize, Deserialize, Display (strum), Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord, Debug):
   - `Severity` enum: Fatal, Error, Warning, Info
   - `ErrorCategory` enum (9): Provider, Channel, Storage, Security, Config, Mcp, Skill, Migration, Internal
   - `FailureMode` enum (8): Network, Auth, RateLimit, Timeout, Validation, Internal, ResourceExhausted, Unavailable
   - `ProviderErrorKind` enum (5): RateLimited, AuthFailed, ServerError, Timeout, ModelNotFound
   - `ChannelErrorKind` enum (5): DeliveryFailed, ConnectionLost, RateLimited, MessageTooLarge, UnsupportedContent
   - `SkillErrorKind` enum (4): ExecutionFailed, CapabilityDenied, SandboxTimeout, CompilationFailed
   - `StorageErrorKind` enum (5): Busy, Corruption, SchemaError, DiskFull, ConnectionFailed
   - `McpErrorKind` enum (5): ConnectionFailed, ToolExecutionFailed, Timeout, AuthFailed, ProtocolError
   - `MigrationErrorKind` enum (3): SchemaFailed, DataCorruption, VersionMismatch

3. Define `ErrorContext` struct (Debug, Clone, Default):
   - http_status: Option<u16>
   - provider_name: Option<String>
   - channel_name: Option<String>
   - retry_after: Option<std::time::Duration>
   - request_id: Option<String>

4. Refactor BlufioError variant shapes (6 variants change, 10 unchanged per CONTEXT.md):
   - Provider { kind: ProviderErrorKind, context: ErrorContext, source: Option<Box<dyn Error + Send + Sync>> }
   - Channel { kind: ChannelErrorKind, context: ErrorContext, source: Option<Box<dyn Error + Send + Sync>> }
   - Storage { kind: StorageErrorKind, context: ErrorContext, source: Box<dyn Error + Send + Sync> }
   - Skill { kind: SkillErrorKind, context: ErrorContext, source: Option<Box<dyn Error + Send + Sync>> }
   - Mcp { kind: McpErrorKind, context: ErrorContext, source: Option<Box<dyn Error + Send + Sync>> }
   - Migration { kind: MigrationErrorKind, context: ErrorContext }
   Update thiserror #[error("...")] attributes to reference context fields.

5. Implement 7 inherent methods on BlufioError:
   - `is_retryable() -> bool`: derived from failure_mode() -- Network/RateLimit/Timeout/Unavailable = true
   - `severity() -> Severity`: match per variant+kind using mapping from CONTEXT.md (Fatal: Config/ResourceExhausted; Error: Auth/Network/RateLimit/Timeout/Unavailable/Internal; Warning: Validation; Info: ModelNotFound)
   - `category() -> ErrorCategory`: direct variant-to-category mapping
   - `failure_mode() -> FailureMode`: match variant+kind per CONTEXT.md mapping
   - `trips_circuit_breaker() -> bool`: true for Network/RateLimit/Timeout/Unavailable/ServerError failure modes; false for Auth/Config/Validation/ResourceExhausted/Internal
   - `suggested_backoff() -> Option<Duration>`: RateLimit uses retry_after or 5s, Network 1s, Timeout 2s, Unavailable 3s; None for non-retryable
   - `user_message() -> Cow<'static, str>`: sanitized English messages, no provider names/HTTP codes/paths

6. Implement `FailureMode::is_retryable(&self) -> bool` for the proptest invariant.

7. Add `http_status_to_provider_error(status: u16, provider_name: &str) -> ProviderErrorKind` free function per RESEARCH.md pattern (429->RateLimited, 401/403->AuthFailed, 404->ModelNotFound, 408->Timeout, 529+anthropic->RateLimited, 500/502/503/504/529->ServerError).

8. Add constructor helpers (construct only, no side effects):
   - `BlufioError::provider_rate_limited(retry_after: Option<Duration>, provider_name: &str) -> Self`
   - `BlufioError::provider_auth_failed(provider_name: &str) -> Self`
   - `BlufioError::provider_timeout(provider_name: &str) -> Self`
   - `BlufioError::provider_server_error(provider_name: &str, source: impl Error) -> Self`
   - `BlufioError::provider_model_not_found(model: &str, provider_name: &str) -> Self`
   - `BlufioError::provider_from_http(status: u16, provider_name: &str, source: Option<Box<dyn Error + Send + Sync>>) -> Self`
   - `BlufioError::channel_delivery_failed(channel_name: &str, source: impl Error) -> Self`
   - `BlufioError::channel_connection_lost(channel_name: &str) -> Self`
   - `BlufioError::channel_rate_limited(channel_name: &str, retry_after: Option<Duration>) -> Self`
   - `BlufioError::channel_message_too_large(channel_name: &str) -> Self`
   - `BlufioError::channel_unsupported_content(channel_name: &str) -> Self`
   - `BlufioError::storage_busy(source: impl Error) -> Self`
   - `BlufioError::storage_corruption(source: impl Error) -> Self`
   - `BlufioError::mcp_connection_failed(source: impl Error) -> Self`
   - `BlufioError::mcp_tool_failed(source: impl Error) -> Self`
   - `BlufioError::skill_execution_failed(source: impl Error) -> Self`
   - `BlufioError::migration_schema_failed(msg: &str) -> Self`
   - Plus temporary deprecated fallback constructors: `provider_generic(message, source)`, `channel_generic(message, source)`, `storage_generic(source)`, `skill_generic(message, source)`, `mcp_generic(message, source)`, `migration_generic(message)` -- these map to ServerError/DeliveryFailed/etc. defaults so downstream crates compile.

9. Define `error_log!` macro per RESEARCH.md design -- dispatches to tracing::error!/warn!/info! based on severity, includes category, failure_mode, retryable as structured fields.

10. Update blufio-core/src/lib.rs re-exports to include all new public types.

CRITICAL: After changing variant shapes, run `cargo check --workspace` -- expect errors in downstream crates. The temporary fallback constructors should cover most construction sites. Fix any remaining compile errors in blufio-core itself. Downstream crate errors are expected and will be fixed in Plans 02/03.
  </action>
  <verify>
    <automated>cd /Users/suman/projects/github/blufio && cargo test -p blufio-core --lib 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 sub-enums, ErrorContext, 7 classification methods, constructor helpers (including deprecated fallbacks), error_log! macro, and http_status_to_provider_error() exist in error.rs. `cargo test -p blufio-core` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ChannelCapabilities with 4 new typed fields and update all construction sites</name>
  <files>crates/blufio-core/src/types.rs, crates/blufio-core/src/lib.rs</files>
  <action>
1. In types.rs, define new types (all with appropriate derives including Serialize, Deserialize, Debug, Clone, Copy, PartialEq, and #[non_exhaustive] on enums):
   - `StreamingType` enum: None, EditBased, AppendOnly
   - `FormattingSupport` enum: PlainText, BasicMarkdown, FullMarkdown, HTML
   - `RateLimit` struct: messages_per_second: Option<f32>, burst_limit: Option<u32>, daily_limit: Option<u32>
   - Implement Default for StreamingType (None), FormattingSupport (PlainText), RateLimit (all None)

2. Add 4 new fields to ChannelCapabilities struct:
   - streaming_type: StreamingType (default: None)
   - formatting_support: FormattingSupport (default: PlainText)
   - rate_limit: Option<RateLimit> (default: None)
   - supports_code_blocks: bool (default: false)

3. Ensure ChannelCapabilities derives Default (or has a manual Default impl) with conservative defaults for all 13 fields (9 existing + 4 new). All existing tests that construct ChannelCapabilities must use `..Default::default()` or have the new fields added explicitly.

4. Update all ChannelCapabilities construction sites within blufio-core (tests, format.rs) to include new fields.

5. Update lib.rs re-exports for StreamingType, FormattingSupport, RateLimit.

6. Run `cargo check -p blufio-core` -- must pass. Downstream adapter crates will fail until Plan 03 updates them, but blufio-core itself must be clean.
  </action>
  <verify>
    <automated>cd /Users/suman/projects/github/blufio && cargo test -p blufio-core 2>&1 | tail -20</automated>
  </verify>
  <done>ChannelCapabilities has 13 fields (9 original + 4 new). StreamingType, FormattingSupport, RateLimit types exist with correct derives. All blufio-core tests pass with new fields.</done>
</task>

</tasks>

<verification>
- `cargo test -p blufio-core` passes (all classification method tests, type construction tests)
- All 6 sub-enums exist and are matchable
- All 7 methods exist on BlufioError
- ChannelCapabilities has 4 new typed fields
- Temporary fallback constructors are marked #[deprecated]
</verification>

<success_criteria>
- blufio-core compiles and all its tests pass
- Every BlufioError variant has correct is_retryable/severity/category/failure_mode/trips_circuit_breaker classification
- Constructor helpers exist for all common error creation patterns
- error_log! macro dispatches to correct tracing level with structured fields
- ChannelCapabilities extended with StreamingType, FormattingSupport, RateLimit, supports_code_blocks
</success_criteria>

<output>
After completion, create `.planning/phases/46-core-types-error-hierarchy/46-01-SUMMARY.md`
</output>
