---
phase: 28-close-audit-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - .planning/phases/25-sqlcipher-database-encryption/25-VERIFICATION.md
  - .planning/phases/27-self-update-with-rollback/27-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [CIPH-01, CIPH-02, CIPH-03, CIPH-04, CIPH-05, CIPH-06, CIPH-07, CIPH-08, UPDT-01, UPDT-02, UPDT-03, UPDT-04, UPDT-05, UPDT-06, UPDT-07, UPDT-08]

must_haves:
  truths:
    - "Cargo.toml line 29 uses bundled-sqlcipher-vendored-openssl feature flag"
    - "cargo check exits 0 after the feature flag change"
    - "25-VERIFICATION.md exists with PASS for all 8 CIPH requirements citing concrete code locations"
    - "27-VERIFICATION.md exists with PASS for all 8 UPDT requirements citing concrete code locations"
  artifacts:
    - path: "Cargo.toml"
      provides: "Fixed CIPH-01 feature flag"
      contains: "bundled-sqlcipher-vendored-openssl"
    - path: ".planning/phases/25-sqlcipher-database-encryption/25-VERIFICATION.md"
      provides: "Phase 25 verification report"
      contains: "8/8 requirements verified"
    - path: ".planning/phases/27-self-update-with-rollback/27-VERIFICATION.md"
      provides: "Phase 27 verification report"
      contains: "8/8 requirements verified"
  key_links:
    - from: "Cargo.toml"
      to: "25-VERIFICATION.md CIPH-01 evidence"
      via: "Feature flag change documented as evidence"
    - from: "v1.2-MILESTONE-AUDIT.md evidence"
      to: "25-VERIFICATION.md + 27-VERIFICATION.md"
      via: "Audit integration checker evidence transcribed into verification files"
---

<objective>
Fix CIPH-01 feature flag in Cargo.toml and create missing verification files for phases 25 and 27.

Purpose: The v1.2 milestone audit found that CIPH-01 uses the wrong feature flag (bundled-sqlcipher instead of bundled-sqlcipher-vendored-openssl) and that phases 25 and 27 lack VERIFICATION.md files. This plan fixes the one real code deviation and creates the two missing verification documents, using audit evidence to populate them.

Output: Corrected Cargo.toml, 25-VERIFICATION.md (8/8 CIPH requirements), 27-VERIFICATION.md (8/8 UPDT requirements)
</objective>

<execution_context>
@/Users/suman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/suman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.planning/phases/24-sd-notify-integration/24-VERIFICATION.md
@.planning/phases/26-minisign-signature-verification/26-VERIFICATION.md
@.planning/phases/25-sqlcipher-database-encryption/25-01-SUMMARY.md
@.planning/phases/25-sqlcipher-database-encryption/25-02-SUMMARY.md
@.planning/phases/25-sqlcipher-database-encryption/25-03-SUMMARY.md
@.planning/phases/25-sqlcipher-database-encryption/25-04-SUMMARY.md
@.planning/phases/27-self-update-with-rollback/27-01-SUMMARY.md
@.planning/phases/27-self-update-with-rollback/27-02-SUMMARY.md
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CIPH-01 feature flag in Cargo.toml and verify compilation</name>
  <files>Cargo.toml</files>
  <action>
In the workspace Cargo.toml at line 29, change:
```toml
rusqlite = { version = "0.37", features = ["bundled-sqlcipher"] }
```
to:
```toml
rusqlite = { version = "0.37", features = ["bundled-sqlcipher-vendored-openssl"] }
```

After the edit, run `cargo check` to validate compilation succeeds. The vendored-openssl feature compiles OpenSSL from source, which on macOS should work out of the box.

If `cargo check` fails:
- Check error messages for openssl-sys, cc, or cmake issues
- Do NOT proceed to verification files until this compiles
- Investigate and fix the root cause

Record the cargo check exit code and last 10 lines of output for inclusion in the verification file.
  </action>
  <verify>
    <automated>cd /Users/suman/projects/github/blufio && grep 'bundled-sqlcipher-vendored-openssl' Cargo.toml && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>Cargo.toml uses bundled-sqlcipher-vendored-openssl feature flag and cargo check exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Create 25-VERIFICATION.md for SQLCipher Database Encryption</name>
  <files>.planning/phases/25-sqlcipher-database-encryption/25-VERIFICATION.md</files>
  <action>
Create 25-VERIFICATION.md following the established format from 24-VERIFICATION.md and 26-VERIFICATION.md.

**Frontmatter:**
```yaml
---
phase: 25-sqlcipher-database-encryption
status: passed
verified: 2026-03-04
---
```

**Phase Goal:** "Operator can encrypt the database at rest so that a stolen disk or backup file reveals nothing without the encryption key"

**Requirement Verification Table:** Verify all 8 CIPH requirements. For each requirement, cite concrete code evidence. The audit's integration checker already confirmed all are wired. Inspect the actual source files to confirm evidence is accurate:

| ID | Evidence source |
|----|----------------|
| CIPH-01 | Cargo.toml line 29 — now `bundled-sqlcipher-vendored-openssl` (fixed in this phase); `cargo check` exits 0 |
| CIPH-02 | `apply_encryption_key()` called as first `conn.call()` in both `open_connection()` and `open_connection_sync()` in `crates/blufio-storage/src/database.rs` |
| CIPH-03 | `apply_encryption_key()` reads `std::env::var("BLUFIO_DB_KEY")` in `crates/blufio-storage/src/database.rs` |
| CIPH-04 | `verify_key()` runs `SELECT count(*) FROM sqlite_master` immediately after `apply_encryption_key()` in `crates/blufio-storage/src/database.rs` |
| CIPH-05 | Integration checker confirmed zero `tokio_rusqlite::Connection::open` in production code outside factory. Grep `crates/` for `Connection::open` to verify — only factory usages exist |
| CIPH-06 | `Commands::Db` dispatches to `DbCommands::Encrypt` which calls `encrypt::run_encrypt()` in `crates/blufio/src/main.rs` |
| CIPH-07 | `backup.rs` uses `open_connection_sync()` for all file-based connections, passing encryption key to both source and destination |
| CIPH-08 | `check_encryption()` in `crates/blufio/src/doctor.rs` with 4-way diagnostic (encrypted+key, encrypted+no-key, plaintext+key, plaintext+no-key) |

**IMPORTANT:** Before writing evidence, grep/read the actual source files to confirm the functions and locations still match. Use the audit evidence as a starting point but verify against current code. Cite file paths and function names — not just "confirmed" or "implemented."

**Must-Have Truths from plans 25-01 through 25-04:** Read each plan's `must_haves.truths` from frontmatter and verify each one. Include a Must-Have Truths section per plan, same format as 24-VERIFICATION.md.

**Score:** 8/8 requirements verified.
  </action>
  <verify>
    <automated>test -f /Users/suman/projects/github/blufio/.planning/phases/25-sqlcipher-database-encryption/25-VERIFICATION.md && grep -c "PASS" /Users/suman/projects/github/blufio/.planning/phases/25-sqlcipher-database-encryption/25-VERIFICATION.md</automated>
  </verify>
  <done>25-VERIFICATION.md exists with PASS verdicts for all 8 CIPH requirements, concrete code evidence per requirement, and must-have truths from all 4 plans verified</done>
</task>

<task type="auto">
  <name>Task 3: Create 27-VERIFICATION.md for Self-Update with Rollback</name>
  <files>.planning/phases/27-self-update-with-rollback/27-VERIFICATION.md</files>
  <action>
Create 27-VERIFICATION.md following the established format from 24-VERIFICATION.md and 26-VERIFICATION.md.

**Frontmatter:**
```yaml
---
phase: 27-self-update-with-rollback
status: passed
verified: 2026-03-04
---
```

**Phase Goal:** "Operator can update Blufio in place with a single command, with signature verification and automatic rollback if the new binary is broken"

**Requirement Verification Table:** Verify all 8 UPDT requirements. For each, cite concrete code evidence. The audit's integration checker confirmed all are wired. Inspect the actual source files to confirm:

| ID | Evidence source |
|----|----------------|
| UPDT-01 | `fetch_latest_release()` in `crates/blufio/src/update.rs` queries GitHub Releases API |
| UPDT-02 | `download_to_temp()` downloads platform binary and `.minisig` to temp files in `update.rs` |
| UPDT-03 | `verify_download()` calls `blufio_verify::verify_signature()` before `backup_current()`/`self_replace()` |
| UPDT-04 | `backup_current()` copies binary to `.bak` + `self_replace::self_replace()` performs atomic swap |
| UPDT-05 | `health_check()` spawns `blufio doctor` subprocess with 30s timeout |
| UPDT-06 | `run_rollback()` -> `do_rollback()` renames `.bak` back to binary path |
| UPDT-07 | `run_check()` fetches latest version and compares without downloading |
| UPDT-08 | `confirm_update()` checks `IsTerminal` and prompts `[y/N]`; `--yes` flag bypasses |

**IMPORTANT:** Before writing evidence, grep/read `crates/blufio/src/update.rs` to confirm the functions and locations still match. Cite file paths and function names.

**Must-Have Truths from plans 27-01 and 27-02:** Read each plan's `must_haves.truths` from frontmatter and verify each one. Include a Must-Have Truths section per plan, same format as 24-VERIFICATION.md.

**Score:** 8/8 requirements verified.
  </action>
  <verify>
    <automated>test -f /Users/suman/projects/github/blufio/.planning/phases/27-self-update-with-rollback/27-VERIFICATION.md && grep -c "PASS" /Users/suman/projects/github/blufio/.planning/phases/27-self-update-with-rollback/27-VERIFICATION.md</automated>
  </verify>
  <done>27-VERIFICATION.md exists with PASS verdicts for all 8 UPDT requirements, concrete code evidence per requirement, and must-have truths from both plans verified</done>
</task>

</tasks>

<verification>
1. `grep 'bundled-sqlcipher-vendored-openssl' Cargo.toml` returns a match
2. `cargo check` exits 0
3. `test -f .planning/phases/25-sqlcipher-database-encryption/25-VERIFICATION.md` returns 0
4. `test -f .planning/phases/27-self-update-with-rollback/27-VERIFICATION.md` returns 0
5. Both verification files contain exactly 8 PASS verdicts each
6. Both verification files cite concrete file paths and function names as evidence
</verification>

<success_criteria>
- Cargo.toml uses `bundled-sqlcipher-vendored-openssl` and compilation succeeds
- 25-VERIFICATION.md verifies CIPH-01..08 with concrete code evidence, all PASS
- 27-VERIFICATION.md verifies UPDT-01..08 with concrete code evidence, all PASS
</success_criteria>

<output>
After completion, create `.planning/phases/28-close-audit-gaps/28-01-SUMMARY.md`
</output>
