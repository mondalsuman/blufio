---
phase: 63-code-quality-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/blufio/src/serve.rs
  - crates/blufio/src/serve/mod.rs
  - crates/blufio/src/serve/storage.rs
  - crates/blufio/src/serve/channels.rs
  - crates/blufio/src/serve/gateway.rs
  - crates/blufio/src/serve/subsystems.rs
  - crates/blufio/src/main.rs
  - crates/blufio/src/cli/mod.rs
  - crates/blufio/src/cli/config_cmd.rs
  - crates/blufio/src/cli/cron_cmd.rs
  - crates/blufio/src/cli/gdpr_cmd.rs
  - crates/blufio/src/cli/audit_cmd.rs
  - crates/blufio-gateway/src/handlers/status.rs
autonomous: true
requirements:
  - QUAL-03
  - QUAL-04
  - QUAL-05

must_haves:
  truths:
    - "serve.rs is decomposed into serve/ directory with focused modules under 600 lines each"
    - "main.rs CLI handlers are decomposed into cli/ directory modules"
    - "/api/status returns actual uptime (not hardcoded 0)"
    - "Mock provider has no unimplemented!() macro calls"
  artifacts:
    - path: "crates/blufio/src/serve/mod.rs"
      provides: "Orchestrator that calls into sub-modules"
      contains: "mod storage"
    - path: "crates/blufio/src/serve/storage.rs"
      provides: "Database/storage initialization"
    - path: "crates/blufio/src/serve/channels.rs"
      provides: "Channel adapter initialization"
    - path: "crates/blufio/src/serve/gateway.rs"
      provides: "Gateway/API setup"
    - path: "crates/blufio/src/serve/subsystems.rs"
      provides: "Subsystem startup (cron, hooks, hot-reload, etc.)"
    - path: "crates/blufio/src/cli/mod.rs"
      provides: "CLI dispatch module"
  key_links:
    - from: "crates/blufio/src/serve/mod.rs"
      to: "crates/blufio/src/serve/storage.rs"
      via: "mod + function calls"
      pattern: "mod storage"
    - from: "crates/blufio/src/main.rs"
      to: "crates/blufio/src/cli/mod.rs"
      via: "mod cli + dispatch calls"
      pattern: "cli::"
---

<objective>
Decompose the two largest files in the binary crate (serve.rs at 2,331 lines and main.rs at 3,220 lines) into focused module directories, fix the hardcoded uptime in /api/status, and verify mock provider has no unimplemented!() calls.

Purpose: Smaller files make the subsequent unwrap sweep (Plans 02-03) faster and less error-prone. Quick fixes bundled here because they touch the same files.
Output: serve/ directory (4-5 modules), cli/ directory (5+ modules), working /api/status with real uptime.
</objective>

<execution_context>
@/Users/smxaz7/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smxaz7/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-code-quality-hardening/63-CONTEXT.md

@crates/blufio/src/serve.rs
@crates/blufio/src/main.rs
@crates/blufio-gateway/src/handlers/status.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose serve.rs into serve/ directory</name>
  <files>crates/blufio/src/serve.rs, crates/blufio/src/serve/mod.rs, crates/blufio/src/serve/storage.rs, crates/blufio/src/serve/channels.rs, crates/blufio/src/serve/gateway.rs, crates/blufio/src/serve/subsystems.rs</files>
  <action>
1. Create `crates/blufio/src/serve/` directory.
2. Read serve.rs fully and identify logical groups:
   - **storage.rs**: Database opening, migrations, SqliteStorage creation, Litestream pragma setup
   - **channels.rs**: All channel adapter initialization (Telegram, Discord, Slack, WhatsApp, Signal, IRC, Matrix, Email, iMessage, SMS) -- each behind its feature gate
   - **gateway.rs**: Gateway builder setup, AppState construction, route mounting, Swagger UI, TLS config. Fix QUAL-03 here: add `start_time: Instant` to AppState (or the shared state struct used by the status handler), set it to `Instant::now()` at serve startup, and pass it through so the status handler computes actual uptime via `start_time.elapsed().as_secs()`
   - **subsystems.rs**: EventBus setup, audit trail init, cron scheduler, hook manager, hot reload watcher, injection pipeline, resilience subsystem
   - **mod.rs**: The `pub async fn serve(...)` orchestrator that calls `init_storage()`, `init_channels()`, `init_gateway()`, `init_subsystems()` etc. in order. Keep the top-level flow readable.
3. Delete the original serve.rs file after creating serve/mod.rs.
4. Each module gets a brief `//!` doc comment (1-2 lines).
5. Functions that were `pub` or used across modules stay `pub(crate)` or `pub(super)` as appropriate.
6. For QUAL-03: Find how AppState or the shared state is structured in blufio-gateway. Add an `uptime_start: Instant` field (or similar). In the status handler, compute `uptime_secs: start_time.elapsed().as_secs()`. The exact approach depends on the existing AppState struct -- inspect it and choose the minimal change.
7. For QUAL-04: Run `grep -rn 'unimplemented!' crates/` to check for mock provider stubs. If found, replace with proper stubs returning default/error values. If none found (likely already resolved), note in summary.
8. Pure refactor: move code largely unchanged. Fix dead code or redundant clones only if trivially obvious.

Verify the module compiles: `cargo check -p blufio 2>&1 | tail -20`
  </action>
  <verify>
    <automated>cargo check -p blufio 2>&1 | tail -30</automated>
  </verify>
  <done>serve.rs replaced by serve/ directory with 4-5 focused modules each under 600 lines. /api/status returns real uptime. No unimplemented!() in mock providers.</done>
</task>

<task type="auto">
  <name>Task 2: Decompose main.rs into cli/ directory</name>
  <files>crates/blufio/src/main.rs, crates/blufio/src/cli/mod.rs, crates/blufio/src/cli/config_cmd.rs, crates/blufio/src/cli/cron_cmd.rs, crates/blufio/src/cli/gdpr_cmd.rs, crates/blufio/src/cli/audit_cmd.rs</files>
  <action>
1. Create `crates/blufio/src/cli/` directory.
2. Read main.rs fully. Keep clap struct definitions (Cli, Commands enum, all subcommand structs) in main.rs per user decision.
3. Move handler functions into cli/ modules grouped by subcommand:
   - **config_cmd.rs**: `blufio config` handlers (show, edit, validate, etc.)
   - **cron_cmd.rs**: `blufio cron` handlers (list, add, remove, run-now, history, generate-timers)
   - **gdpr_cmd.rs**: `blufio gdpr` handlers (erase, report, export, list-users)
   - **audit_cmd.rs**: `blufio audit` handlers (verify, tail, stats)
   - Additional modules as logical groups emerge (e.g., litestream_cmd.rs, memory_cmd.rs, doctor_cmd.rs, etc.). Group by subcommand -- each module handles one top-level subcommand.
   - **mod.rs**: Re-exports all handler modules. May contain shared CLI utilities.
4. In main.rs, the match arms in the command dispatcher now call `cli::config_cmd::handle_config(...)`, `cli::cron_cmd::handle_cron(...)`, etc.
5. Each module gets `//!` doc comment.
6. Delete dead code found during decomposition. Preserve all existing behavior.
7. Run unused dep scan: `cargo machete 2>&1 | head -30` (if cargo-machete is installed). Log findings but don't fix unless obvious.

Verify full workspace: `cargo check --workspace 2>&1 | tail -20` and `cargo test --workspace --no-run 2>&1 | tail -20`
  </action>
  <verify>
    <automated>cargo check --workspace 2>&1 | tail -30</automated>
  </verify>
  <done>main.rs contains only clap structs + dispatch logic. All handler implementations live in cli/ modules. Workspace compiles clean.</done>
</task>

</tasks>

<verification>
- `cargo check --workspace` passes with no errors
- `cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -20` passes
- `cargo test --workspace 2>&1 | tail -30` -- all existing tests pass
- serve/ directory exists with mod.rs + 4 sub-modules
- cli/ directory exists with mod.rs + 4+ sub-modules
- No `unimplemented!()` in production code: `grep -rn 'unimplemented!' crates/ --include='*.rs' | grep -v test | grep -v '#\[cfg(test)\]'`
</verification>

<success_criteria>
- serve.rs decomposed into serve/ with each module under 600 lines
- main.rs reduced to clap structs + dispatch (under 500 lines ideally)
- /api/status returns actual uptime_secs (not hardcoded 0)
- Mock provider has no unimplemented!() stubs
- All existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/63-code-quality-hardening/63-01-SUMMARY.md`
</output>
