---
phase: 63-code-quality-hardening
plan: 03
type: execute
wave: 2
depends_on: ["63-01"]
files_modified:
  - crates/blufio-agent/src/lib.rs
  - crates/blufio-anthropic/src/lib.rs
  - crates/blufio-auth-keypair/src/lib.rs
  - crates/blufio-bridge/src/lib.rs
  - crates/blufio-bus/src/lib.rs
  - crates/blufio-context/src/lib.rs
  - crates/blufio-core/src/lib.rs
  - crates/blufio-cost/src/lib.rs
  - crates/blufio-cron/src/lib.rs
  - crates/blufio-discord/src/lib.rs
  - crates/blufio-email/src/lib.rs
  - crates/blufio-gateway/src/lib.rs
  - crates/blufio-gdpr/src/lib.rs
  - crates/blufio-gemini/src/lib.rs
  - crates/blufio-hooks/src/lib.rs
  - crates/blufio-imessage/src/lib.rs
  - crates/blufio-injection/src/lib.rs
  - crates/blufio-irc/src/lib.rs
  - crates/blufio-matrix/src/lib.rs
  - crates/blufio-mcp-client/src/lib.rs
  - crates/blufio-mcp-server/src/lib.rs
  - crates/blufio-node/src/lib.rs
  - crates/blufio-ollama/src/lib.rs
  - crates/blufio-openai/src/lib.rs
  - crates/blufio-openrouter/src/lib.rs
  - crates/blufio-plugin/src/lib.rs
  - crates/blufio-prometheus/src/lib.rs
  - crates/blufio-resilience/src/lib.rs
  - crates/blufio-router/src/lib.rs
  - crates/blufio-security/src/lib.rs
  - crates/blufio-signal/src/lib.rs
  - crates/blufio-slack/src/lib.rs
  - crates/blufio-sms/src/lib.rs
  - crates/blufio-telegram/src/lib.rs
  - crates/blufio-verify/src/lib.rs
  - crates/blufio-whatsapp/src/lib.rs
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02

must_haves:
  truths:
    - "#![deny(clippy::unwrap_used)] is enforced across ALL library crates (35 total)"
    - "All unwrap() calls in library crates are replaced with proper error handling"
    - "Workspace-wide cargo clippy passes clean with deny directive active in all lib crates"
  artifacts:
    - path: "crates/blufio-agent/src/lib.rs"
      provides: "deny directive example (representative)"
      contains: "deny(clippy::unwrap_used)"
    - path: "crates/blufio-gateway/src/lib.rs"
      provides: "deny directive example (representative)"
      contains: "deny(clippy::unwrap_used)"
  key_links:
    - from: "crates/*/src/lib.rs"
      to: "cargo clippy --workspace"
      via: "deny directive in every lib crate"
      pattern: "deny.*clippy::unwrap_used"
---

<objective>
Replace all unwrap() calls and add #![deny(clippy::unwrap_used)] to the remaining ~29 library crates not covered in Plan 02. Most of these crates have fewer unwrap() calls (under 30 each), making them faster to sweep.

Purpose: Complete QUAL-01/QUAL-02 -- every library crate in the workspace enforces no-unwrap policy.
Output: All 35 library crates with deny directive, zero unwrap() in non-test production code.
</objective>

<execution_context>
@/Users/smxaz7/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smxaz7/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-code-quality-hardening/63-CONTEXT.md
@.planning/phases/63-code-quality-hardening/63-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unwrap sweep -- core infrastructure crates (15 crates)</name>
  <files>crates/blufio-agent/src/**, crates/blufio-bus/src/**, crates/blufio-context/src/**, crates/blufio-core/src/**, crates/blufio-cost/src/**, crates/blufio-cron/src/**, crates/blufio-gateway/src/**, crates/blufio-gdpr/src/**, crates/blufio-hooks/src/**, crates/blufio-injection/src/**, crates/blufio-mcp-client/src/**, crates/blufio-mcp-server/src/**, crates/blufio-node/src/**, crates/blufio-plugin/src/**, crates/blufio-resilience/src/**</files>
  <action>
For each of the 15 core infrastructure crates, apply the same procedure as Plan 02:

1. Add `#![deny(clippy::unwrap_used)]` to lib.rs.
2. Run `cargo clippy -p {crate_name}` to find flagged unwraps.
3. Replace using decision patterns (see 63-CONTEXT.md):
   - Mutex/RwLock -> `.expect("lock poisoned")`
   - Regex literals -> `.expect("valid regex: name")`
   - Channel send -> `if let Err(e) = send() { tracing::warn!(...) }`
   - serde_json / DB parse -> `?` propagation
   - Option -> `.ok_or_else(|| BlufioError::Internal(...))?`
   - Crypto invariants -> `.expect("reason")`
4. DO NOT touch #[cfg(test)] code.
5. Verify each crate passes clippy and tests before moving to next.

Crate list: blufio-agent, blufio-bus, blufio-context, blufio-core, blufio-cost, blufio-cron, blufio-gateway, blufio-gdpr, blufio-hooks, blufio-injection, blufio-mcp-client, blufio-mcp-server, blufio-node, blufio-plugin, blufio-resilience.

Process in batches of 3-5 crates, running clippy after each batch.
  </action>
  <verify>
    <automated>cargo clippy -p blufio-agent -p blufio-bus -p blufio-context -p blufio-core -p blufio-cost -p blufio-cron -p blufio-gateway -p blufio-gdpr -p blufio-hooks -p blufio-injection -p blufio-mcp-client -p blufio-mcp-server -p blufio-node -p blufio-plugin -p blufio-resilience -- -D warnings 2>&1 | tail -30</automated>
  </verify>
  <done>15 core infrastructure crates have deny(clippy::unwrap_used) and pass clippy clean.</done>
</task>

<task type="auto">
  <name>Task 2: Unwrap sweep -- provider + channel + remaining crates (14 crates)</name>
  <files>crates/blufio-anthropic/src/**, crates/blufio-auth-keypair/src/**, crates/blufio-bridge/src/**, crates/blufio-discord/src/**, crates/blufio-email/src/**, crates/blufio-gemini/src/**, crates/blufio-imessage/src/**, crates/blufio-irc/src/**, crates/blufio-matrix/src/**, crates/blufio-ollama/src/**, crates/blufio-openai/src/**, crates/blufio-openrouter/src/**, crates/blufio-prometheus/src/**, crates/blufio-router/src/**, crates/blufio-security/src/**, crates/blufio-signal/src/**, crates/blufio-slack/src/**, crates/blufio-sms/src/**, crates/blufio-telegram/src/**, crates/blufio-verify/src/**, crates/blufio-whatsapp/src/**</files>
  <action>
Same procedure for remaining library crates:

1. Add `#![deny(clippy::unwrap_used)]` to each lib.rs.
2. Fix all unwrap() calls per decision patterns.
3. Channel adapter crates (discord, email, imessage, irc, matrix, signal, slack, sms, telegram, whatsapp): Map external SDK errors to `BlufioError::Channel(format!("adapter: {err}"))` -- no new error variants per user decision.
4. Provider crates (anthropic, gemini, ollama, openai, openrouter): HTTP response parsing unwraps -> propagate as provider errors.
5. After all crates done, run full workspace verification:
   - `cargo clippy --workspace --all-targets -- -D warnings`
   - `cargo test --workspace`
6. Count total unwrap() remaining in library crates (should be 0 in non-test code):
   `grep -rn '\.unwrap()' crates/ --include='*.rs' | grep -v '/test' | grep -v '#\[cfg(test)\]' | grep -v 'blufio/src/' | wc -l`
   (The `grep -v 'blufio/src/'` excludes the binary crate which keeps unwrap per decision.)

Crate list: blufio-anthropic, blufio-auth-keypair, blufio-bridge, blufio-discord, blufio-email, blufio-gemini, blufio-imessage, blufio-irc, blufio-matrix, blufio-ollama, blufio-openai, blufio-openrouter, blufio-prometheus, blufio-router, blufio-security, blufio-signal, blufio-slack, blufio-sms, blufio-telegram, blufio-verify, blufio-whatsapp.
  </action>
  <verify>
    <automated>cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -30</automated>
  </verify>
  <done>All ~35 library crates have deny(clippy::unwrap_used). Workspace clippy and tests pass clean. QUAL-01 and QUAL-02 fully complete.</done>
</task>

</tasks>

<verification>
- `cargo clippy --workspace --all-targets -- -D warnings` passes clean
- `cargo test --workspace` -- all tests pass
- Every library crate lib.rs contains `deny(clippy::unwrap_used)`: `grep -rn 'deny(clippy::unwrap_used)' crates/*/src/lib.rs | wc -l` should equal 35+
- Binary crate (crates/blufio/src/main.rs) does NOT have the deny directive
</verification>

<success_criteria>
- All 35 library crates have #![deny(clippy::unwrap_used)]
- Zero clippy warnings across entire workspace
- All existing tests pass
- unwrap() count in non-test library code is 0
</success_criteria>

<output>
After completion, create `.planning/phases/63-code-quality-hardening/63-03-SUMMARY.md`
</output>
