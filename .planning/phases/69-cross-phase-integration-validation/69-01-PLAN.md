---
phase: 69-cross-phase-integration-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/blufio-gdpr/src/erasure.rs
  - crates/blufio-cron/src/tasks/memory_cleanup.rs
  - contrib/blufio.example.toml
  - crates/blufio/tests/e2e_integration.rs
autonomous: true
requirements:
  - VEC-05
must_haves:
  truths:
    - "GDPR erasure removes memories from vec0 table -- no ghost entries survive"
    - "Cron memory cleanup syncs vec0 status -- doctor check shows no drift"
    - "blufio.example.toml documents all v1.6 config sections"
    - "Cross-subsystem integration tests pass covering GDPR+vec0, compaction+vec0, doctor checks, EventBus events, config loading"
  artifacts:
    - path: "crates/blufio-gdpr/src/erasure.rs"
      provides: "Vec0 cleanup before memories DELETE"
      contains: "memories_vec0"
    - path: "crates/blufio-cron/src/tasks/memory_cleanup.rs"
      provides: "Vec0 status sync during cron cleanup"
      contains: "memories_vec0"
    - path: "contrib/blufio.example.toml"
      provides: "v1.6 config documentation"
      contains: "vec0_enabled"
    - path: "crates/blufio/tests/e2e_integration.rs"
      provides: "Cross-subsystem integration tests"
      min_lines: 100
  key_links:
    - from: "crates/blufio-gdpr/src/erasure.rs"
      to: "memories_vec0 table"
      via: "DELETE FROM memories_vec0 before DELETE FROM memories"
      pattern: "DELETE FROM memories_vec0"
    - from: "crates/blufio-cron/src/tasks/memory_cleanup.rs"
      to: "memories_vec0 table"
      via: "UPDATE vec0 status alongside memories UPDATE"
      pattern: "memories_vec0"
---

<objective>
Fix discovered wiring gaps (GDPR erasure, cron cleanup, example TOML) and create cross-subsystem integration tests validating vec0 interacts correctly with GDPR, compaction, config, EventBus, and doctor checks.

Purpose: Wiring gaps found during research (erasure.rs missing vec0 DELETE, memory_cleanup.rs bypassing vec0 sync) are real production bugs. Fixing them first enables integration tests to validate correct behavior.
Output: Fixed wiring in erasure.rs + memory_cleanup.rs, updated example TOML, new e2e_integration.rs with 6+ cross-subsystem tests.
</objective>

<execution_context>
@/Users/smxaz7/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smxaz7/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-cross-phase-integration-validation/69-RESEARCH.md

Key source files to read before implementing:
- crates/blufio-gdpr/src/erasure.rs (line 90-98: DELETE FROM memories without vec0)
- crates/blufio-cron/src/tasks/memory_cleanup.rs (line 64: raw SQL UPDATE without vec0)
- contrib/blufio.example.toml (missing [memory] section)
- crates/blufio/tests/e2e_vec0.rs (reuse setup_test_db pattern, synthetic_embedding, make_test_memory, vec0_count_async)
- crates/blufio-bus/src/events.rs (BusEvent enum, Vec0PopulationComplete, SecurityEvent)
- crates/blufio/src/doctor.rs (check_vec0, check_injection_defense)
- crates/blufio-memory/src/store.rs (MemoryStore with vec0_enabled, save, batch_evict, soft_delete)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix wiring gaps -- GDPR erasure vec0 sync, cron cleanup vec0 sync, example TOML</name>
  <files>crates/blufio-gdpr/src/erasure.rs, crates/blufio-cron/src/tasks/memory_cleanup.rs, contrib/blufio.example.toml</files>
  <action>
1. **erasure.rs**: Before the `DELETE FROM memories WHERE session_id IN (...)` statement (~line 90-98), add `DELETE FROM memories_vec0 WHERE rowid IN (SELECT rowid FROM memories WHERE session_id IN (...))`. The vec0 DELETE must come BEFORE the memories DELETE because the rowid subquery depends on memories rows still existing. Wrap both in the same transaction. Guard with `IF EXISTS` or catch the "no such table" error gracefully for databases without vec0 enabled.

2. **memory_cleanup.rs**: At ~line 64 where it does `UPDATE memories SET deleted_at = ...`, add a corresponding `UPDATE memories_vec0 SET status = 'evicted' WHERE rowid IN (SELECT rowid FROM memories WHERE ...)` using the same WHERE clause. This ensures vec0 status stays in sync. Same guard for databases without vec0 table.

3. **blufio.example.toml**: Add a commented `[memory]` section documenting `vec0_enabled = true` (default), and any other v1.6 config fields (injection_defense severity weights section if missing). Review existing sections and add comments for any v1.6 additions.

Run `cargo check -p blufio-gdpr -p blufio-cron` to verify compilation.
  </action>
  <verify>
    <automated>cd /Users/smxaz7/Desktop/Suman/Projects/gh/blufio && cargo check -p blufio-gdpr -p blufio-cron</automated>
  </verify>
  <done>erasure.rs deletes from memories_vec0 before memories DELETE, memory_cleanup.rs syncs vec0 status during cron soft-delete, blufio.example.toml has [memory] section with vec0_enabled documented. All three files compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create cross-subsystem integration tests (e2e_integration.rs)</name>
  <files>crates/blufio/tests/e2e_integration.rs</files>
  <action>
Create new integration test file. Copy the setup_test_db() pattern from e2e_vec0.rs (in-memory SQLite with full schema including sessions, memories, FTS5 triggers, vec0 virtual table). Also copy synthetic_embedding(), make_test_memory(), vec0_count_async() helpers. Do NOT import from e2e_vec0.rs -- Rust test files are not library modules, so copy the helpers.

Write these targeted tests (per CONTEXT.md locked decisions):

1. **test_gdpr_erasure_with_vec0_sync**: Save 5 memories with vec0, call the erasure SQL path (or simulate with same DELETE statements as erasure.rs), verify vec0_count drops to 0 and KNN search returns empty.

2. **test_compaction_vec0_consistency**: Save memories, update one memory's status to 'superseded' (simulating compaction), update vec0 metadata status to match, verify KNN search with status='active' filter excludes the superseded memory.

3. **test_cron_cleanup_vec0_sync**: Save memories, run the cron cleanup SQL path (UPDATE memories SET deleted_at + UPDATE memories_vec0 SET status), verify vec0 KNN search excludes cleaned-up memories.

4. **test_doctor_checks_v16_subsystems**: Unit-level test calling doctor health check functions (check_vec0 sync drift verification, check injection defense). May need to test at the SQL level if doctor functions require full runtime.

5. **test_eventbus_v16_events**: Create EventBus, subscribe, emit Vec0PopulationComplete and SecurityEvent variants, verify they are received via tokio::time::timeout (1 second). Per RESEARCH.md Pattern 4.

6. **test_toml_config_all_v16_sections**: Load a complete BlufioConfig from a TOML string containing all v1.6 sections (memory.vec0_enabled, injection_defense weights, benchmark settings). Verify deserialization succeeds and all fields have expected values.

7. **test_bench_cli_smoke** (if feasible without full binary): Verify bench CLI argument parsing accepts --only binary_size and --only memory_profile with vec0_enabled=true config. May be a config-level test rather than full binary invocation.

8. **test_prometheus_metric_names_unique**: This can be a compile-time or grep-based check. If more natural as a verification step, move to VERIFICATION.md instead.

Use #[tokio::test] for async tests. Use assert! with descriptive messages. Each test should be self-contained with its own setup_test_db() call.
  </action>
  <verify>
    <automated>cd /Users/smxaz7/Desktop/Suman/Projects/gh/blufio && cargo test -p blufio --test e2e_integration -- --test-threads=1</automated>
  </verify>
  <done>e2e_integration.rs has 6+ passing cross-subsystem tests covering GDPR+vec0, compaction+vec0, cron+vec0, EventBus events, config loading, and doctor checks. All tests use in-memory SQLite and pass in under 30 seconds.</done>
</task>

</tasks>

<verification>
- `cargo test -p blufio --test e2e_integration` passes all tests
- `cargo check -p blufio-gdpr -p blufio-cron` compiles cleanly
- `cargo clippy -p blufio-gdpr -p blufio-cron -p blufio` -- no new warnings
- contrib/blufio.example.toml contains [memory] section with vec0_enabled
</verification>

<success_criteria>
- Wiring gaps fixed: GDPR erasure syncs vec0, cron cleanup syncs vec0, example TOML updated
- 6+ cross-subsystem integration tests pass covering vec0 interactions with GDPR, compaction, cron, EventBus, config, doctor
- No regressions in existing tests (cargo test --workspace passes)
</success_criteria>

<output>
After completion, create `.planning/phases/69-cross-phase-integration-validation/69-01-SUMMARY.md`
</output>
