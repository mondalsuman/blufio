---
phase: 58-cron-scheduler-retention-policies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/blufio-cron/Cargo.toml
  - crates/blufio-cron/src/lib.rs
  - crates/blufio-cron/src/tasks/mod.rs
  - crates/blufio-config/src/model.rs
  - crates/blufio-bus/src/events.rs
  - crates/blufio-storage/migrations/V14__cron_retention.sql
  - Cargo.toml
autonomous: true
requirements:
  - CRON-01
  - CRON-06
  - RETN-01
  - RETN-03

must_haves:
  truths:
    - "CronConfig and RetentionConfig parse from TOML with serde(default)"
    - "CronEvent variant exists on BusEvent enum"
    - "V14 migration creates cron_jobs, cron_history tables and adds deleted_at to 4 tables"
    - "blufio-cron crate compiles with CronTask trait exported"
  artifacts:
    - path: "crates/blufio-cron/Cargo.toml"
      provides: "Crate manifest with croner, workspace deps"
    - path: "crates/blufio-cron/src/lib.rs"
      provides: "Public API re-exports"
    - path: "crates/blufio-cron/src/tasks/mod.rs"
      provides: "CronTask trait definition"
      exports: ["CronTask", "CronTaskError"]
    - path: "crates/blufio-config/src/model.rs"
      provides: "CronConfig, RetentionConfig, CronJobConfig, RetentionPeriods"
    - path: "crates/blufio-bus/src/events.rs"
      provides: "CronEvent variant on BusEvent"
    - path: "crates/blufio-storage/migrations/V14__cron_retention.sql"
      provides: "cron_jobs, cron_history tables + deleted_at columns"
  key_links:
    - from: "crates/blufio-cron/Cargo.toml"
      to: "Cargo.toml"
      via: "workspace member"
      pattern: "blufio-cron"
    - from: "crates/blufio-config/src/model.rs"
      to: "BlufioConfig"
      via: "serde(default) fields"
      pattern: "pub cron: CronConfig"
---

<objective>
Create the foundation types for cron scheduling and retention policies: new blufio-cron crate with CronTask trait, config models in blufio-config, CronEvent on EventBus, and V14 SQLite migration with cron tables and soft-delete columns.

Purpose: Establishes the type contracts and database schema that all subsequent plans build against.
Output: Compilable blufio-cron crate scaffold, config types, event type, migration SQL.
</objective>

<execution_context>
@/Users/smxaz7/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smxaz7/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-cron-scheduler-retention-policies/58-RESEARCH.md

@crates/blufio-config/src/model.rs
@crates/blufio-bus/src/events.rs
@crates/blufio-storage/migrations/
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create blufio-cron crate, config types, CronEvent, and V14 migration</name>
  <files>
    crates/blufio-cron/Cargo.toml,
    crates/blufio-cron/src/lib.rs,
    crates/blufio-cron/src/tasks/mod.rs,
    crates/blufio-config/src/model.rs,
    crates/blufio-bus/src/events.rs,
    crates/blufio-storage/migrations/V14__cron_retention.sql,
    Cargo.toml
  </files>
  <action>
1. **Workspace Cargo.toml:** Add `croner = "3"` to workspace dependencies. Add `"crates/blufio-cron"` to workspace members. Add `blufio-cron = { path = "crates/blufio-cron" }` to workspace dependencies.

2. **crates/blufio-cron/Cargo.toml:** Create new crate manifest:
   - Dependencies: croner (workspace), chrono (workspace), serde (workspace), tokio (workspace), tokio-rusqlite (workspace), rusqlite (workspace), tokio-util (workspace), async-trait (workspace), tracing (workspace), thiserror (workspace), blufio-bus (workspace), blufio-config (workspace), blufio-storage (workspace), blufio-core (workspace)
   - Follow existing crate Cargo.toml pattern (e.g., blufio-injection/Cargo.toml)

3. **crates/blufio-cron/src/tasks/mod.rs:** Define the CronTask trait:
   ```rust
   #[async_trait]
   pub trait CronTask: Send + Sync {
       fn name(&self) -> &str;
       fn description(&self) -> &str;
       async fn execute(&self) -> Result<String, CronTaskError>;
       fn timeout(&self) -> std::time::Duration {
           std::time::Duration::from_secs(300)
       }
   }
   ```
   Define `CronTaskError` using thiserror with variants: Timeout, DatabaseError(String), ExecutionError(String), Other(String).

4. **crates/blufio-cron/src/lib.rs:** Re-export CronTask, CronTaskError from tasks module. Declare modules: `pub mod tasks;`

5. **crates/blufio-config/src/model.rs:** Add to BlufioConfig struct (with `#[serde(default)]`):
   - `pub cron: CronConfig` field
   - `pub retention: RetentionConfig` field

   Define structs following existing pattern (deny_unknown_fields on each):
   - `CronConfig { enabled: bool, job_timeout_secs: u64 (default 300), max_history: usize (default 1000), jobs: Vec<CronJobConfig> }`
   - `CronJobConfig { name: String, schedule: String, task: String, enabled: bool (default true) }`
   - `RetentionConfig { enabled: bool, grace_period_days: u64 (default 7), periods: RetentionPeriods, restricted: RetentionPeriods }`
   - `RetentionPeriods { messages: Option<u64>, sessions: Option<u64>, cost_records: Option<u64>, memories: Option<u64> }` -- all default None (no retention)

   All structs need `#[derive(Debug, Clone, Deserialize, Serialize)]` and proper Default impls. Use `default_*` functions following the existing pattern in model.rs.

6. **crates/blufio-bus/src/events.rs:** Add `Cron(CronEvent)` variant to BusEvent enum. Define CronEvent enum (String fields, no cross-crate deps):
   ```rust
   #[derive(Debug, Clone, Serialize, Deserialize)]
   pub enum CronEvent {
       Completed { event_id: String, timestamp: String, job_name: String, status: String, duration_ms: u64 },
       Failed { event_id: String, timestamp: String, job_name: String, error: String },
   }
   ```
   Add `impl fmt::Display for CronEvent` following existing event Display pattern. Add EventFilter match arm for "cron.*" in the filter matching logic.

7. **crates/blufio-storage/migrations/V14__cron_retention.sql:** Create migration exactly as specified in RESEARCH.md:
   - CREATE TABLE cron_jobs (name PRIMARY KEY, schedule, task, enabled, running, last_run_at, created_at, updated_at)
   - CREATE TABLE cron_history (id AUTOINCREMENT, job_name, started_at, finished_at, status, duration_ms, output, created_at)
   - Indexes on cron_history(job_name) and cron_history(started_at)
   - ALTER TABLE messages/sessions/cost_ledger/memories ADD COLUMN deleted_at TEXT
   - Indexes on deleted_at for each table
  </action>
  <verify>
    <automated>cd /Users/smxaz7/Desktop/Suman/Projects/gh/blufio && cargo check -p blufio-cron -p blufio-config -p blufio-bus 2>&1 | tail -5</automated>
  </verify>
  <done>blufio-cron crate exists with CronTask trait, CronConfig/RetentionConfig parse from TOML, CronEvent on BusEvent, V14 migration ready. `cargo check` passes for all three crates.</done>
</task>

<task type="auto">
  <name>Task 2: Add soft-delete filtering to existing storage queries</name>
  <files>
    crates/blufio-storage/src/sqlite.rs,
    crates/blufio-storage/src/adapter.rs,
    crates/blufio-memory/src/store.rs
  </files>
  <action>
After V14 adds `deleted_at` columns, existing SELECT queries MUST filter on `deleted_at IS NULL` to avoid returning soft-deleted records. This is critical per RESEARCH.md Pitfall 5.

1. **crates/blufio-storage/src/sqlite.rs:** Find all SELECT queries against messages, sessions, cost_ledger tables. Add `AND deleted_at IS NULL` to their WHERE clauses. Key queries to update:
   - Message retrieval (get_messages, get_session_messages, etc.)
   - Session listing (get_sessions, get_session, etc.)
   - Cost record queries (get_cost_records, etc.)
   Do NOT modify INSERT/UPDATE queries. Do NOT modify any query that is explicitly for retention/cleanup purposes.

2. **crates/blufio-storage/src/adapter.rs:** If there are trait-level queries here that hit these tables, add the same filter.

3. **crates/blufio-memory/src/store.rs:** Find SELECT queries against the memories table. Add `AND deleted_at IS NULL` to WHERE clauses for memory retrieval, search, and listing. The FTS5 queries that join memories table also need this filter on the memories side of the join.

NOTE: Be thorough -- grep for all SELECT statements touching these 4 tables across the entire codebase. Any SELECT that returns data to users/consumers must exclude soft-deleted records.
  </action>
  <verify>
    <automated>cd /Users/smxaz7/Desktop/Suman/Projects/gh/blufio && cargo test --workspace 2>&1 | tail -10</automated>
  </verify>
  <done>All existing read queries for messages, sessions, cost_ledger, and memories filter out soft-deleted records (deleted_at IS NULL). Workspace tests pass.</done>
</task>

</tasks>

<verification>
- `cargo check -p blufio-cron` compiles without errors
- `cargo check -p blufio-config` compiles with new CronConfig/RetentionConfig
- `cargo check -p blufio-bus` compiles with CronEvent
- `cargo test --workspace` passes (no regressions from soft-delete filters)
- V14 migration SQL is syntactically valid
</verification>

<success_criteria>
- blufio-cron crate exists as workspace member with CronTask trait
- CronConfig and RetentionConfig are fields on BlufioConfig with serde(default)
- CronEvent is a variant on BusEvent with Completed/Failed sub-variants
- V14 migration creates cron_jobs, cron_history tables and adds deleted_at to messages/sessions/cost_ledger/memories
- All existing read queries exclude soft-deleted records
</success_criteria>

<output>
After completion, create `.planning/phases/58-cron-scheduler-retention-policies/58-01-SUMMARY.md`
</output>
