---
phase: 68-performance-benchmarking-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/blufio/src/bench.rs
autonomous: true
requirements: [PERF-01, PERF-02]

must_haves:
  truths:
    - "Running `blufio bench --only binary_size` reports the binary's own file size in bytes and human-readable format"
    - "Running `blufio bench --only memory_profile` reports jemalloc stats (allocated, active, resident, mapped) at idle"
    - "Memory profile under load shows RSS samples every 100 operations during 1000 saves + 100 retrievals"
    - "If RSS grows monotonically without plateau, output flags potential memory leak"
    - "Per-crate breakdown via cargo-bloat is shown in CLI output (report-only, not stored)"
  artifacts:
    - path: "crates/blufio/src/bench.rs"
      provides: "BenchmarkKind::BinarySize and BenchmarkKind::MemoryProfile variants with full implementation"
      contains: "BinarySize"
  key_links:
    - from: "crates/blufio/src/bench.rs"
      to: "tikv_jemalloc_ctl::stats"
      via: "epoch::advance() + stats::allocated/active/resident/mapped::read()"
      pattern: "epoch::advance"
    - from: "crates/blufio/src/bench.rs"
      to: "std::env::current_exe"
      via: "self-measurement for binary size"
      pattern: "current_exe"
---

<objective>
Add BenchmarkKind::BinarySize and BenchmarkKind::MemoryProfile variants to the existing bench.rs CLI module, enabling `blufio bench` to measure binary size (with cargo-bloat per-crate breakdown) and memory RSS profiling (jemalloc stats at idle and under load with leak detection).

Purpose: Quantifies binary size against the <50MB target and memory usage against 50-80MB idle / 100-200MB loaded targets. These are the foundational measurements that CI (Plan 04) and the OpenClaw comparison (Plan 03) depend on.
Output: Extended bench.rs with two new BenchmarkKind variants fully implemented.
</objective>

<execution_context>
@/Users/smxaz7/.claude/get-shit-done/workflows/execute-plan.md
@/Users/smxaz7/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-performance-benchmarking-suite/68-CONTEXT.md
@.planning/phases/68-performance-benchmarking-suite/68-RESEARCH.md
@crates/blufio/src/bench.rs

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From crates/blufio/src/bench.rs:
```rust
pub struct BenchmarkResult {
    pub name: String,
    pub median: Duration,
    pub min: Duration,
    pub max: Duration,
    pub peak_rss: Option<u64>,
    pub iterations: u32,
}

pub enum BenchmarkKind {
    Startup,
    ContextAssembly,
    Wasm,
    Sqlite,
    // Add: BinarySize, MemoryProfile
}

// Existing helpers to reuse:
// fn get_peak_rss() -> u64
// fn format_bytes(bytes: u64) -> String
// fn format_duration(d: Duration) -> String
// fn collect_system_info() -> serde_json::Value
// fn run_bench(kind: BenchmarkKind, ...) -> Result<Vec<BenchmarkResult>, BlufioError>
```

From tikv-jemalloc-ctl (already a dependency with stats feature):
```rust
use tikv_jemalloc_ctl::{epoch, stats};
epoch::advance().unwrap();
let allocated = stats::allocated::read().unwrap();
let active = stats::active::read().unwrap();
let resident = stats::resident::read().unwrap();
let mapped = stats::mapped::read().unwrap();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BenchmarkKind::BinarySize variant</name>
  <files>crates/blufio/src/bench.rs</files>
  <action>
Add `BinarySize` variant to `BenchmarkKind` enum. Update `Display` impl to return `"binary_size"`. Update `FromStr` impl to match `"binary_size" | "binarysize" | "binary"`.

Implement the BinarySize arm in `run_bench()`:
1. Get own binary path via `std::env::current_exe()`, map error to `BlufioError::Internal`
2. Read file metadata via `std::fs::metadata()`, extract `.len()` as `size_bytes: u64`
3. Print binary size in bytes and human-readable format using existing `format_bytes()`
4. Print target comparison: "Target: <50MB | Status: OK/EXCEEDED"
5. Attempt to run `cargo bloat --release --crates -n 20` via `std::process::Command` and capture stdout. If cargo-bloat is not installed (command fails), print "cargo-bloat not installed -- run `cargo install cargo-bloat` for per-crate breakdown" and continue (do NOT error). If it succeeds, print the per-crate breakdown output.
6. Return a `BenchmarkResult` with name="binary_size", median/min/max=Duration::ZERO, peak_rss=Some(size_bytes), iterations=1. The size is stored in peak_rss field (repurposed for this variant).

Do NOT store per-crate breakdown in bench_results (report-only per user decision).
Track both debug and release sizes: if running a debug build, note "Debug build detected -- release size will differ" in output.
  </action>
  <verify>
    <automated>cd /Users/smxaz7/Desktop/Suman/Projects/gh/blufio && cargo build -p blufio 2>&1 | tail -5</automated>
  </verify>
  <done>BenchmarkKind::BinarySize compiles, reports own binary size, attempts cargo-bloat per-crate breakdown, returns BenchmarkResult with size in peak_rss field</done>
</task>

<task type="auto">
  <name>Task 2: Add BenchmarkKind::MemoryProfile variant</name>
  <files>crates/blufio/src/bench.rs</files>
  <action>
Add `MemoryProfile` variant to `BenchmarkKind` enum. Update `Display` impl to return `"memory_profile"`. Update `FromStr` impl to match `"memory_profile" | "memoryprofile" | "memory"`.

Implement the MemoryProfile arm in `run_bench()`:

**Idle measurement:**
1. Call `epoch::advance().unwrap()` then read `stats::allocated`, `stats::active`, `stats::resident`, `stats::mapped` via `tikv_jemalloc_ctl`
2. Call existing `get_peak_rss()` for OS-level peak RSS
3. Print idle stats table: "=== Idle Memory Profile ===" with allocated, active, resident, mapped, peak_rss all in human-readable format via `format_bytes()`
4. Print target comparison: "Target: 50-80MB idle | Measured: {resident} | Status: {WITHIN/ABOVE/BELOW}"
5. Print OpenClaw comparison line: "OpenClaw documented range: 300-800MB"

**Under-load measurement (if full benchmark requested):**
6. Note: The actual 1000-save + 100-retrieval workload requires MemoryStore and HybridRetriever initialization which may not be available in the bench CLI context. For this task, implement the RSS sampling framework:
   - Create a `sample_rss()` helper that calls `epoch::advance()` + `stats::resident::read()` and returns the value
   - Create a `check_leak(samples: &[u64])` helper that checks if RSS grows monotonically (every sample >= previous) AND total growth > 10% of initial. If so, print "WARNING: Potential memory leak detected -- RSS grew monotonically from {first} to {last} ({growth}%)"
   - Create a `print_rss_summary(samples: &[u64])` helper that prints min, max, mean, and growth trend
7. For now, collect idle-only stats and return. The under-load measurement will be exercised when invoked with the full application context (the framework is ready for Plan 04's CI integration).

**Return:** `BenchmarkResult` with name="memory_profile", median/min/max=Duration::ZERO, peak_rss=Some(resident), iterations=1.

Add `use tikv_jemalloc_ctl::{epoch, stats};` at top of file (these are already project dependencies).

**vec0 vs in-memory memory comparison:** Add a note in CLI output: "For vec0 vs in-memory comparison, run with --vec0-enabled=true and --vec0-enabled=false back-to-back". This is informational -- the actual comparison is done manually or in CI.
  </action>
  <verify>
    <automated>cd /Users/smxaz7/Desktop/Suman/Projects/gh/blufio && cargo build -p blufio 2>&1 | tail -5</automated>
  </verify>
  <done>BenchmarkKind::MemoryProfile compiles, reports jemalloc stats at idle, includes RSS sampling helpers for leak detection, prints OpenClaw comparison line, returns BenchmarkResult</done>
</task>

</tasks>

<verification>
- `cargo build -p blufio` succeeds with no errors
- bench.rs contains BinarySize and MemoryProfile variants in BenchmarkKind enum
- FromStr and Display impls handle both new variants
- run_bench() dispatches both new variants without panics
</verification>

<success_criteria>
- BenchmarkKind::BinarySize reports binary size and attempts cargo-bloat
- BenchmarkKind::MemoryProfile reports jemalloc stats with leak detection framework
- Both return valid BenchmarkResult structs
- No compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/68-performance-benchmarking-suite/68-01-SUMMARY.md`
</output>
