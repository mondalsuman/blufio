---
phase: 16-mcp-server-stdio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/blufio-config/src/model.rs
  - crates/blufio-mcp-server/src/lib.rs
  - crates/blufio-mcp-server/src/bridge.rs
  - crates/blufio-mcp-server/Cargo.toml
autonomous: true
requirements: [SRVR-12]

must_haves:
  truths:
    - "bridge::filtered_tools() excludes 'bash' from results regardless of export_tools config"
    - "bridge::filtered_tools() with empty export_tools returns all non-bash tools"
    - "bridge::filtered_tools() with explicit allowlist returns only listed tools (minus bash)"
    - "bridge::to_mcp_tool() converts Blufio ToolRegistry entries to rmcp Tool structs"
    - "McpConfig has tool_timeout_secs field defaulting to 60"
  artifacts:
    - crates/blufio-mcp-server/src/bridge.rs
  key_links:
    - "bridge.rs uses ToolRegistry from blufio-skill to produce rmcp Tool types"
    - "McpConfig.tool_timeout_secs used by handler for tool invocation timeout"
---

<objective>
Implement the tool registry bridge and export allowlist filtering.

Purpose: Bridge Blufio's ToolRegistry (Anthropic-format tool definitions) to MCP's tool schema format (rmcp Tool structs) and enforce the export allowlist with bash permanently excluded (SRVR-12).

Output: `bridge.rs` module with tool filtering and schema conversion functions. Updated `McpConfig` with `tool_timeout_secs` field.
</objective>

<execution_context>
@/Users/suman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/suman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-mcp-server-stdio/16-RESEARCH.md
@.planning/phases/16-mcp-server-stdio/16-CONTEXT.md
@crates/blufio-skill/src/tool.rs
@crates/blufio-config/src/model.rs
@crates/blufio-mcp-server/src/lib.rs
@crates/blufio-mcp-server/Cargo.toml

<interfaces>
<!-- From blufio-skill/src/tool.rs: -->
pub trait Tool: Send + Sync {
    fn name(&self) -> &str;
    fn description(&self) -> &str;
    fn parameters_schema(&self) -> serde_json::Value;
    async fn invoke(&self, input: serde_json::Value) -> Result<ToolOutput, BlufioError>;
}

pub struct ToolRegistry { tools: HashMap<String, Arc<dyn Tool>>, builtin_names: HashSet<String> }
impl ToolRegistry {
    pub fn get(&self, name: &str) -> Option<Arc<dyn Tool>>;
    pub fn list(&self) -> Vec<(&str, &str)>;
    pub fn tool_definitions(&self) -> Vec<serde_json::Value>;
}

pub struct ToolOutput { pub content: String, pub is_error: bool }

<!-- From blufio-config/src/model.rs: -->
pub struct McpConfig {
    pub enabled: bool,
    pub servers: Vec<McpServerEntry>,
    pub export_tools: Vec<String>,
}

<!-- From rmcp (via Context7 research): -->
// rmcp::model::Tool has: name, description, input_schema (ToolInputSchema)
// ToolInputSchema has: type_ (String), properties (serde_json::Map), required (Vec<String>)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tool_timeout_secs to McpConfig and create bridge module</name>
  <files>
    crates/blufio-config/src/model.rs,
    crates/blufio-mcp-server/src/bridge.rs,
    crates/blufio-mcp-server/src/lib.rs,
    crates/blufio-mcp-server/Cargo.toml
  </files>
  <behavior>
    - Test: empty export_tools with bash in registry returns all tools except bash
    - Test: explicit export_tools=["http","file"] returns only http and file
    - Test: export_tools=["bash","http"] returns only http (bash silently excluded)
    - Test: export_tools=["nonexistent"] returns empty list (no panic)
    - Test: to_mcp_tool converts a Tool with name/description/parameters_schema to rmcp::model::Tool
    - Test: to_mcp_tool preserves properties and required fields from JSON Schema
    - Test: McpConfig default tool_timeout_secs is 60
    - Test: McpConfig with tool_timeout_secs = 30 parses correctly
  </behavior>
  <action>
1. In `crates/blufio-config/src/model.rs`, add `tool_timeout_secs: u64` to `McpConfig`:
   ```rust
   pub struct McpConfig {
       #[serde(default)]
       pub enabled: bool,
       #[serde(default)]
       pub servers: Vec<McpServerEntry>,
       #[serde(default)]
       pub export_tools: Vec<String>,
       #[serde(default = "default_tool_timeout_secs")]
       pub tool_timeout_secs: u64,
   }
   ```
   Add `fn default_tool_timeout_secs() -> u64 { 60 }`.
   Update the existing `Default` impl if there is one (McpConfig derives Default, so may need manual impl instead).

2. In `crates/blufio-mcp-server/Cargo.toml`, verify `rmcp` dep has features `["server", "macros", "transport-io"]` (already present). No changes needed if correct.

3. Create `crates/blufio-mcp-server/src/bridge.rs`:

   - `pub fn filtered_tool_names(registry: &ToolRegistry, export_tools: &[String]) -> Vec<String>`:
     - If export_tools is empty: return all tool names from `registry.list()` EXCEPT "bash"
     - If export_tools is non-empty: return names that are in export_tools AND in registry, EXCEPT "bash"
     - Log warning if "bash" was in export_tools: `tracing::warn!("'bash' in mcp.export_tools is ignored (security: never exported)")`

   - `pub fn to_mcp_tool(name: &str, tool: &dyn Tool) -> rmcp::model::Tool`:
     - Convert tool.parameters_schema() (serde_json::Value) to rmcp's tool schema
     - Use `rmcp::model::Tool { name, description, input_schema }` construction
     - Extract `properties` and `required` from the JSON Schema value
     - Handle missing properties/required gracefully (empty defaults)

4. In `crates/blufio-mcp-server/src/lib.rs`:
   - Add `pub mod bridge;`
   - Keep existing `pub mod types;` and `pub use types::McpSessionId;`

5. Write comprehensive tests in bridge.rs:
   - Create test tools (reuse EchoTool pattern from blufio-skill/src/tool.rs tests)
   - Test all filtering edge cases from behavior list
   - Test schema conversion correctness
  </action>
  <verify>
    <automated>cargo test -p blufio-mcp-server -- --nocapture</automated>
  </verify>
  <done>
    - bridge::filtered_tool_names correctly filters bash in all configurations
    - bridge::to_mcp_tool produces valid rmcp Tool structs from Blufio tools
    - McpConfig has tool_timeout_secs defaulting to 60
    - All tests pass
    - cargo clippy -p blufio-mcp-server passes with no warnings
  </done>
</task>

</tasks>

<verification>
- `cargo test -p blufio-mcp-server` passes all tests
- `cargo test -p blufio-config` passes (McpConfig changes don't break existing tests)
- `cargo clippy -p blufio-mcp-server -p blufio-config` clean
</verification>

<success_criteria>
- bridge.rs exists with filtered_tool_names and to_mcp_tool functions
- bash is NEVER included in filtered results (security critical)
- McpConfig.tool_timeout_secs field exists with 60s default
- All edge cases tested (empty allowlist, explicit allowlist, bash in allowlist)
</success_criteria>

<output>
After completion, create `.planning/phases/16-mcp-server-stdio/16-01-SUMMARY.md`
</output>
