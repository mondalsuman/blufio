---
phase: 19-integration-testing-tech-debt
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - crates/blufio/tests/e2e_mcp_client.rs
autonomous: true
requirements: [INTG-02]

must_haves:
  truths:
    - "E2E test passes: agent uses an external MCP tool in a conversation turn end-to-end"
    - "External tool is discovered via tools/list and registered with namespace prefix"
    - "Tool invocation result appears in the agent pipeline"
  artifacts:
    - path: "crates/blufio/tests/e2e_mcp_client.rs"
      provides: "MCP client E2E test"
      contains: "external_tool"
  key_links:
    - from: "crates/blufio/tests/e2e_mcp_client.rs"
      to: "blufio_mcp_client::McpClientManager"
      via: "connect_all() and tool registry"
    - from: "crates/blufio/tests/e2e_mcp_client.rs"
      to: "blufio_skill::ToolRegistry"
      via: "namespace-prefixed tool discovery"
---

<objective>
Create an E2E test verifying the MCP client workflow: agent discovers and invokes an external MCP tool.

Purpose: INTG-02 requires an end-to-end test proving the full MCP client path works: configure server -> connect -> discover tools -> register with namespace -> invoke in conversation turn -> receive result.
Output: New test file demonstrating external MCP tool usage in the agent pipeline.
</objective>

<execution_context>
@/Users/suman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/suman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@crates/blufio/tests/e2e.rs
@crates/blufio-mcp-client/src/manager.rs
@crates/blufio-mcp-client/src/external_tool.rs
@crates/blufio-mcp-client/src/lib.rs
@crates/blufio-skill/src/registry.rs
@crates/blufio-config/src/model.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCP client E2E test with mock external server</name>
  <files>crates/blufio/tests/e2e_mcp_client.rs</files>
  <behavior>
    - Test: McpClientManager connects to a local mock MCP server and discovers tools
    - Test: Discovered tools appear in ToolRegistry with namespace prefix (servername___toolname)
    - Test: Invoking the external tool through the registry returns the expected result
    - Test: Connection to unreachable server fails gracefully (non-fatal)
  </behavior>
  <action>
    Create `crates/blufio/tests/e2e_mcp_client.rs` with integration tests for the MCP client workflow.

    **Test approach:** Create a minimal MCP server using rmcp that exposes a simple test tool (e.g., "echo" tool that returns its input). Run it in-process on a random port. Connect via `McpClientManager::connect_all()` and verify tool discovery and invocation.

    **Mock MCP Server setup:**
    Create a minimal rmcp server handler struct that implements the necessary traits:

    ```rust
    use rmcp::{ServerHandler, model::*};
    use rmcp::schemars;

    #[derive(Debug, Clone)]
    struct MockMcpServer;

    // Implement ServerHandler with a single "echo" tool
    // that returns whatever input it receives
    ```

    Start the mock server on a random port:
    ```rust
    let listener = tokio::net::TcpListener::bind("127.0.0.1:0").await.unwrap();
    let port = listener.local_addr().unwrap().port();
    // Spawn the server in background
    ```

    **Tests to implement:**

    1. `test_mcp_client_discovers_external_tools` -- Start mock MCP server with "echo" tool. Create `McpServerEntry` pointing to `http://127.0.0.1:{port}`. Call `McpClientManager::connect_all(&[entry], &tool_registry)`. Verify:
       - Connection result shows 1 connected, 0 failed
       - Tool registry now contains the echo tool with namespace prefix
       - Tool name follows the `servername___echo` convention

    2. `test_mcp_client_invokes_external_tool` -- After connecting (from test 1 setup), invoke the namespaced tool through the ToolRegistry. Verify the result contains the expected echo output.

    3. `test_mcp_client_unreachable_server_graceful` -- Create `McpServerEntry` pointing to a port with no server. Call `McpClientManager::connect_all()`. Verify:
       - Connection result shows 0 connected, 1 failed
       - No panic, no crash
       - Tool registry is unchanged (no tools added)

    **Important notes:**
    - Use `TcpListener::bind("127.0.0.1:0")` for random port assignment
    - Mock server must implement rmcp's Streamable HTTP transport for the client to connect
    - If setting up a full rmcp HTTP server is complex, consider testing at the McpClientManager level with a simpler mock approach -- e.g., mock the HTTP endpoint that responds to MCP JSON-RPC requests using axum
    - The mock server should be started in a tokio::spawn and dropped after the test
    - Feature-gate the test with `#[cfg(feature = "mcp-client")]` if needed
  </action>
  <verify>
    <automated>cargo test --test e2e_mcp_client 2>&1 | tail -20</automated>
  </verify>
  <done>MCP client E2E tests pass: tool discovery with namespace prefix works, tool invocation returns result, unreachable server handled gracefully</done>
</task>

</tasks>

<verification>
- `cargo test --test e2e_mcp_client` -- all MCP client E2E tests pass
- `cargo test --test e2e` -- existing E2E tests still pass
- No hardcoded ports in tests
- Mock server properly cleaned up after tests
</verification>

<success_criteria>
- MCP client connects to external server and discovers tools with namespace prefix
- Tool invocation through registry returns expected result
- Failed connections are non-fatal (agent continues without external tools)
- All tests in-process with random port assignment
</success_criteria>

<output>
After completion, create `.planning/phases/19-integration-testing-tech-debt/19-04-SUMMARY.md`
</output>
