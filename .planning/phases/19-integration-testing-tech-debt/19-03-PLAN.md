---
phase: 19-integration-testing-tech-debt
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - crates/blufio/tests/e2e_mcp_server.rs
  - crates/blufio/tests/e2e_cross_contamination.rs
autonomous: true
requirements: [INTG-01, INTG-03]

must_haves:
  truths:
    - "E2E test passes: MCP handler lists tools, invokes a tool, and reads a memory resource"
    - "JSON-RPC requests to non-MCP endpoints return 4xx"
    - "Gateway-format requests to /mcp return MCP protocol errors"
  artifacts:
    - path: "crates/blufio/tests/e2e_mcp_server.rs"
      provides: "MCP server E2E tests via in-process handler"
      contains: "list_tools"
    - path: "crates/blufio/tests/e2e_cross_contamination.rs"
      provides: "Cross-contamination tests"
      contains: "json_rpc"
  key_links:
    - from: "crates/blufio/tests/e2e_mcp_server.rs"
      to: "blufio_mcp_server::BlufioMcpHandler"
      via: "handler method calls"
    - from: "crates/blufio/tests/e2e_cross_contamination.rs"
      to: "blufio_gateway::server"
      via: "Router::into_service() + tower oneshot"
---

<objective>
Create E2E tests for MCP server workflows and cross-contamination protection.

Purpose: INTG-01 verifies the full MCP server path (connect, list tools, invoke tool, read resource). INTG-03 verifies protocol isolation between JSON-RPC and REST endpoints.
Output: Two new test files exercising MCP server E2E and cross-contamination scenarios.
</objective>

<execution_context>
@/Users/suman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/suman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@crates/blufio/tests/e2e.rs
@crates/blufio-mcp-server/src/handler.rs
@crates/blufio-mcp-server/src/lib.rs
@crates/blufio-mcp-server/src/resources.rs
@crates/blufio-gateway/src/server.rs
@crates/blufio-skill/src/registry.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCP server E2E tests via in-process handler</name>
  <files>crates/blufio/tests/e2e_mcp_server.rs</files>
  <behavior>
    - Test: handler initializes and responds with capabilities (initialize handshake)
    - Test: tools/list returns registered built-in tools
    - Test: tools/call invokes a tool and returns a result
    - Test: resources/list returns available resources (when memory/storage configured)
    - Test: resources/read reads a memory resource
    - Test: invalid tool name returns proper MCP error
  </behavior>
  <action>
    Create `crates/blufio/tests/e2e_mcp_server.rs` with integration tests that exercise `BlufioMcpHandler` in-process. This avoids subprocess management and is consistent with the existing TestHarness pattern used in `e2e.rs`.

    **Test approach:** Create a `BlufioMcpHandler` with a real `ToolRegistry` (with built-in tools registered) and call its handler methods directly via rmcp's `ServerHandler` trait. The handler is already implemented to handle `tools/list`, `tools/call`, `resources/list`, `resources/read`, etc.

    **Setup pattern:**
    ```rust
    use std::sync::Arc;
    use tokio::sync::RwLock;
    use blufio_skill::ToolRegistry;
    use blufio_mcp_server::BlufioMcpHandler;
    use blufio_config::model::McpConfig;

    async fn create_test_handler() -> BlufioMcpHandler {
        let mut registry = ToolRegistry::new();
        blufio_skill::builtin::register_builtins(&mut registry);
        let registry = Arc::new(RwLock::new(registry));
        let mcp_config = McpConfig::default();
        BlufioMcpHandler::new(registry, &mcp_config)
    }
    ```

    **Tests to implement:**

    1. `test_mcp_initialize_handshake` -- Call the initialize method, verify server capabilities include tools and resources.

    2. `test_mcp_list_tools_returns_registered_tools` -- Call tools/list, verify at least one tool is returned, each has name and inputSchema.

    3. `test_mcp_call_tool_returns_result` -- Call tools/call with a valid built-in tool (e.g., the help tool or similar simple tool that doesn't need external dependencies). Verify the response contains content.

    4. `test_mcp_resources_list_with_memory` -- Create handler with `.with_resources(memory_store, storage)`, call resources/list, verify memory and session resources are listed.

    5. `test_mcp_read_resource` -- Create a MemoryStore with a test entry, configure handler with `.with_resources()`, read via `blufio://memory/{id}`.

    6. `test_mcp_call_invalid_tool_returns_error` -- Call tools/call with a nonexistent tool name, verify the response is an error (not a crash).

    Use rmcp's `ServerHandler` trait to call handler methods. Check the handler.rs implementation for the exact method signatures. The handler implements `#[tool_router]` and resource handlers via rmcp macros -- call them through the trait interface.

    If direct trait method invocation is complex due to rmcp's macro-generated code, consider using rmcp's test utilities or creating a mock transport (in-memory pipe using tokio channels) to drive the handler through the protocol layer.
  </action>
  <verify>
    <automated>cargo test --test e2e_mcp_server 2>&1 | tail -20</automated>
  </verify>
  <done>6 MCP server E2E tests pass: initialize, list_tools, call_tool, resources_list, read_resource, invalid_tool_error</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cross-contamination tests</name>
  <files>crates/blufio/tests/e2e_cross_contamination.rs</files>
  <behavior>
    - Test: JSON-RPC request body sent to POST /v1/messages returns 4xx (bad request or unsupported content)
    - Test: Regular REST JSON sent to POST /mcp returns MCP protocol error
    - Test: JSON-RPC request to GET /v1/sessions returns 4xx (method not allowed or bad request)
    - Test: GET request to /mcp without proper MCP session returns error
  </behavior>
  <action>
    Create `crates/blufio/tests/e2e_cross_contamination.rs` with tests that verify protocol isolation. These tests build the gateway Router using axum's test utilities and send requests via `tower::ServiceExt::oneshot()` -- no port binding needed.

    **Setup pattern:**
    ```rust
    use axum::Router;
    use axum::body::Body;
    use http::{Request, StatusCode};
    use tower::ServiceExt;

    fn build_test_app() -> Router {
        // Build the gateway router with MCP nested at /mcp
        // Use a minimal setup with mock state
    }
    ```

    **Tests to implement:**

    1. `test_jsonrpc_to_rest_endpoint_rejected` -- Send a JSON-RPC formatted body `{"jsonrpc":"2.0","method":"tools/list","id":1}` to POST /v1/messages. Verify the response is 4xx (the handler expects `MessageRequest` with `content` field, not JSON-RPC). The deserialization should fail with 422 Unprocessable Entity (axum's default for JSON parse failure) or 400.

    2. `test_rest_to_mcp_endpoint_rejected` -- Send a regular REST body `{"content":"hello"}` to POST /mcp. The MCP endpoint expects JSON-RPC formatted requests. Verify the response is an MCP-level error (JSON-RPC error response with a negative error code) or HTTP 4xx.

    3. `test_jsonrpc_to_sessions_endpoint_rejected` -- Send a JSON-RPC body to GET /v1/sessions (which doesn't accept bodies). Verify 4xx or that the response is still the normal sessions list (GET ignores body -- this test verifies GET semantics are preserved).

    4. `test_mcp_get_without_session_returns_error` -- Send a bare GET to /mcp without MCP session headers. Verify error response (not a 200 with gateway data).

    For the test Router construction, you need to set up GatewayState with appropriate mock/test values. Use the pattern from server.rs tests. The MCP router can be built using `build_mcp_router()` from blufio-mcp-server::transport with a test handler.

    If constructing the full router with MCP is complex, focus on the REST-side tests (verifying JSON-RPC bodies are rejected at REST endpoints) and add a note about the MCP-side test requiring the mcp-server feature.
  </action>
  <verify>
    <automated>cargo test --test e2e_cross_contamination 2>&1 | tail -20</automated>
  </verify>
  <done>Cross-contamination tests pass: JSON-RPC to REST returns 4xx, REST to MCP returns error, protocols are isolated</done>
</task>

</tasks>

<verification>
- `cargo test --test e2e_mcp_server` -- all 6 MCP E2E tests pass
- `cargo test --test e2e_cross_contamination` -- all cross-contamination tests pass
- `cargo test --test e2e` -- existing 7 E2E tests still pass (no regression)
- No hardcoded ports used in any test
</verification>

<success_criteria>
- MCP server E2E: handler initializes, lists tools, invokes tool, lists resources, reads resource, handles invalid tool
- Cross-contamination: JSON-RPC to REST endpoints returns 4xx; REST to /mcp returns error
- All tests are in-process (no subprocess spawning, no port binding)
- Existing E2E tests unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/19-integration-testing-tech-debt/19-03-SUMMARY.md`
</output>
