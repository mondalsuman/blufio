---
phase: 39-integration-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/31-openai-compatible-gateway-api/31-VERIFICATION.md
  - .planning/phases/32-scoped-api-keys-webhooks-batch/32-VERIFICATION.md
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
  - API-07
  - API-08
  - API-09
  - API-10
  - API-11
  - API-12
  - API-13
  - API-14
  - API-15
  - API-16
  - API-17
  - API-18

must_haves:
  truths:
    - "Phase 31 VERIFICATION.md exists with score for all 10 requirements (API-01..10)"
    - "Phase 32 VERIFICATION.md exists with score for all 8 requirements (API-11..18)"
    - "Every requirement has code + test evidence (source file + line, test name)"
    - "Missing test coverage is filled with new tests that pass"
    - "OpenAI wire types are verified as separate from internal ProviderResponse"
  artifacts:
    - path: ".planning/phases/31-openai-compatible-gateway-api/31-VERIFICATION.md"
      provides: "Gateway API verification report"
      contains: "score:"
    - path: ".planning/phases/32-scoped-api-keys-webhooks-batch/32-VERIFICATION.md"
      provides: "API keys, webhooks, batch verification report"
      contains: "score:"
  key_links:
    - from: "31-VERIFICATION.md"
      to: "crates/blufio-gateway/src/openai_compat/"
      via: "evidence references"
      pattern: "blufio-gateway"
    - from: "32-VERIFICATION.md"
      to: "crates/blufio-gateway/src/api_keys/"
      via: "evidence references"
      pattern: "api_keys"
---

<objective>
Verify Phases 31 (OpenAI-Compatible Gateway API) and 32 (Scoped API Keys, Webhooks & Batch) against all 18 API requirements with code + test evidence.

Purpose: Establish formal verification evidence for the entire API layer -- the OpenAI-compatible gateway endpoints, wire type separation, OpenResponses API, tools API, scoped API keys, webhooks with HMAC signing, and batch processing. Note: Phase 32 shows "Not started" in ROADMAP but the research confirmed code exists in blufio-gateway modules.

Output: 31-VERIFICATION.md (new) and 32-VERIFICATION.md (new)
</objective>

<execution_context>
@/Users/suman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/suman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-integration-verification/39-CONTEXT.md
@.planning/phases/39-integration-verification/39-RESEARCH.md

@.planning/phases/31-openai-compatible-gateway-api/31-01-PLAN.md
@.planning/phases/31-openai-compatible-gateway-api/31-02-PLAN.md
@.planning/phases/31-openai-compatible-gateway-api/31-03-PLAN.md
@.planning/phases/31-openai-compatible-gateway-api/31-01-SUMMARY.md
@.planning/phases/31-openai-compatible-gateway-api/31-02-SUMMARY.md
@.planning/phases/31-openai-compatible-gateway-api/31-03-SUMMARY.md

@.planning/phases/32-scoped-api-keys-webhooks-batch/32-01-PLAN.md
@.planning/phases/32-scoped-api-keys-webhooks-batch/32-02-PLAN.md
@.planning/phases/32-scoped-api-keys-webhooks-batch/32-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 31 — OpenAI-Compatible Gateway API (10 requirements)</name>
  <files>.planning/phases/31-openai-compatible-gateway-api/31-VERIFICATION.md</files>
  <action>
Create 31-VERIFICATION.md following the Phase 30 gold standard format:

1. Read all Phase 31 PLANs and SUMMARYs to extract must_haves
2. Run `cargo test -p blufio-gateway` -- record results
3. Read source code in `crates/blufio-gateway/src/openai_compat/` for each requirement:
   - API-01: POST /v1/chat/completions handler -- verify route, request parsing, response format
   - API-02: SSE streaming -- verify streaming response with `data: [DONE]` termination, delta chunks
   - API-03: Tool calling -- verify tools + tool_choice in request, tool_calls in response
   - API-04: response_format (JSON mode) -- verify request field and response handling
   - API-05: usage stats (token counts + cost) -- verify usage field in response
   - API-06: Wire type separation -- verify OpenAiChatResponse uses finish_reason (not stop_reason), no Anthropic field names leak
   - API-07: POST /v1/responses handler -- verify route exists and processes requests
   - API-08: Responses streaming events -- verify response.created, output_text.delta, response.completed
   - API-09: POST /v1/tools/invoke handler -- verify route, tool execution
   - API-10: GET /v1/tools list -- verify route, JSON schema in response
4. Identify test gaps and write missing tests
5. Write 31-VERIFICATION.md with full format

Flag incomplete/stub implementations as UNVERIFIED.
  </action>
  <verify>
    <automated>test -f .planning/phases/31-openai-compatible-gateway-api/31-VERIFICATION.md && grep -c "VERIFIED\|UNVERIFIED" .planning/phases/31-openai-compatible-gateway-api/31-VERIFICATION.md</automated>
  </verify>
  <done>31-VERIFICATION.md exists with score X/10 and evidence for each API-01..10 requirement</done>
</task>

<task type="auto">
  <name>Task 2: Verify Phase 32 — Scoped API Keys, Webhooks & Batch (8 requirements)</name>
  <files>.planning/phases/32-scoped-api-keys-webhooks-batch/32-VERIFICATION.md</files>
  <action>
Create 32-VERIFICATION.md following the Phase 30 gold standard format:

1. Read Phase 32 PLANs (note: no SUMMARYs exist, so verify from code directly)
2. Run `cargo test -p blufio-gateway` -- focus on api_keys, webhooks, batch, rate_limit modules
3. Read source code in `crates/blufio-gateway/src/`:
   - API-11: POST /v1/api-keys -- verify handler in api_keys/ module, key creation with scope restrictions
   - API-12: Scope restrictions (chat.completions, tools.invoke, admin) -- verify scope enforcement in middleware or handler
   - API-13: Per-key rate limiting -- verify rate_limit.rs sliding window implementation, per-key enforcement
   - API-14: Expiration and revocation -- verify expiry check and revocation logic, revoked keys immediately rejected
   - API-15: POST /v1/webhooks -- verify webhook registration handler
   - API-16: HMAC-SHA256 signing + exponential backoff retry -- verify delivery.rs or equivalent
   - API-17: POST /v1/batch -- verify batch submission handler
   - API-18: Per-item success/error results -- verify batch result retrieval
4. Identify test gaps and write missing tests
5. Write 32-VERIFICATION.md with full format

Phase 32 may have partial implementations since ROADMAP shows "Not started" but research confirmed code exists. Be especially thorough -- verify actual functionality, not just file existence.
  </action>
  <verify>
    <automated>test -f .planning/phases/32-scoped-api-keys-webhooks-batch/32-VERIFICATION.md && grep -c "VERIFIED\|UNVERIFIED" .planning/phases/32-scoped-api-keys-webhooks-batch/32-VERIFICATION.md</automated>
  </verify>
  <done>32-VERIFICATION.md exists with score X/8 and evidence for each API-11..18 requirement</done>
</task>

</tasks>

<verification>
- Both VERIFICATION.md files exist with YAML frontmatter and score lines
- Combined coverage: 18 requirements (API-01..18) all have evidence
- `cargo test -p blufio-gateway` passes including any new tests
</verification>

<success_criteria>
- 31-VERIFICATION.md has score X/10 with evidence for API-01..10
- 32-VERIFICATION.md has score X/8 with evidence for API-11..18
- All 18 requirement IDs appear in verification evidence
- Wire type separation (API-06) has explicit evidence showing no Anthropic field names in OpenAI responses
</success_criteria>

<output>
After completion, create `.planning/phases/39-integration-verification/39-02-SUMMARY.md`
</output>
