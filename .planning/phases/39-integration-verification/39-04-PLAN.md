---
phase: 39-integration-verification
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/35-skill-registry-code-signing/35-VERIFICATION.md
  - .planning/phases/36-docker-image-deployment/36-VERIFICATION.md
autonomous: true
requirements:
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - SKILL-04
  - SKILL-05
  - INFRA-04
  - INFRA-05
  - INFRA-07

must_haves:
  truths:
    - "Phase 35 VERIFICATION.md exists with score for all 5 requirements (SKILL-01..05)"
    - "Phase 36 VERIFICATION.md exists with score for all 3 requirements (INFRA-04, INFRA-05, INFRA-07)"
    - "Skill signing verification at install AND execution time has evidence"
    - "Capability enforcement at every WASM host function call site has evidence"
    - "Docker verification uses static analysis since Docker daemon is unavailable; build flagged as UNVERIFIED if needed"
  artifacts:
    - path: ".planning/phases/35-skill-registry-code-signing/35-VERIFICATION.md"
      provides: "Skill registry and code signing verification report"
      contains: "score:"
    - path: ".planning/phases/36-docker-image-deployment/36-VERIFICATION.md"
      provides: "Docker and systemd deployment verification report"
      contains: "score:"
  key_links:
    - from: "35-VERIFICATION.md"
      to: "crates/blufio-skill/src/signing.rs"
      via: "evidence references"
      pattern: "blufio-skill"
    - from: "36-VERIFICATION.md"
      to: "Dockerfile"
      via: "evidence references"
      pattern: "Dockerfile"
---

<objective>
Verify Phases 35 (Skill Registry & Code Signing) and 36 (Docker Image & Deployment) against all 8 requirements.

Purpose: Establish formal verification evidence for the skill registry CLI, SHA-256 content hashing, Ed25519 code signing, pre-execution verification gate, capability enforcement, Docker multi-stage build, docker-compose configuration, and multi-instance systemd template. Docker verification is static-only since Docker daemon is not available on this machine.

Output: 35-VERIFICATION.md (new) and 36-VERIFICATION.md (new, includes Docker static verification)
</objective>

<execution_context>
@/Users/suman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/suman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-integration-verification/39-CONTEXT.md
@.planning/phases/39-integration-verification/39-RESEARCH.md

@.planning/phases/35-skill-registry-code-signing/35-01-PLAN.md
@.planning/phases/35-skill-registry-code-signing/35-02-PLAN.md
@.planning/phases/35-skill-registry-code-signing/35-01-SUMMARY.md
@.planning/phases/35-skill-registry-code-signing/35-02-SUMMARY.md

@.planning/phases/36-docker-image-deployment/36-01-PLAN.md
@.planning/phases/36-docker-image-deployment/36-02-PLAN.md
@.planning/phases/36-docker-image-deployment/36-01-SUMMARY.md
@.planning/phases/36-docker-image-deployment/36-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 35 — Skill Registry & Code Signing (5 requirements)</name>
  <files>.planning/phases/35-skill-registry-code-signing/35-VERIFICATION.md</files>
  <action>
Create 35-VERIFICATION.md following the Phase 30 gold standard format:

1. Read all Phase 35 PLANs and SUMMARYs to extract must_haves
2. Run `cargo test -p blufio-skill` -- record results
3. Read source code for each requirement:
   - SKILL-01: Local skill registry with install/list/remove/update -- verify CLI command handlers in blufio crate (main.rs or skill subcommands) and store operations in `crates/blufio-skill/src/store.rs`
   - SKILL-02: Registry stores skill manifests with SHA-256 content hashes -- verify hash computation and storage in store.rs, manifest struct with hash field
   - SKILL-03: Ed25519 code signing for WASM artifacts -- verify `crates/blufio-skill/src/signing.rs` for sign/verify functions using ed25519-dalek
   - SKILL-04: Signature verification at install time AND before execution -- verify two distinct verification points: install handler calls verify, and `crates/blufio-skill/src/sandbox.rs` pre-execution gate calls verify
   - SKILL-05: Capability enforcement at every WASM host function call site -- verify host function implementations check permissions before executing (not just at install)
4. Identify test gaps -- particularly important for SKILL-04 (dual verification) and SKILL-05 (per-call enforcement)
5. Write 35-VERIFICATION.md with full format

SKILL-04 and SKILL-05 are security-critical. Be extra thorough in verifying these -- check actual call sites, not just function existence.
  </action>
  <verify>
    <automated>test -f .planning/phases/35-skill-registry-code-signing/35-VERIFICATION.md && grep -c "VERIFIED\|UNVERIFIED" .planning/phases/35-skill-registry-code-signing/35-VERIFICATION.md</automated>
  </verify>
  <done>35-VERIFICATION.md exists with score X/5 and evidence for SKILL-01..05, with special attention to dual-verification (SKILL-04) and per-call enforcement (SKILL-05)</done>
</task>

<task type="auto">
  <name>Task 2: Verify Phase 36 — Docker Image & Deployment (3 requirements)</name>
  <files>.planning/phases/36-docker-image-deployment/36-VERIFICATION.md</files>
  <action>
Create 36-VERIFICATION.md following the Phase 30 gold standard format. Docker daemon is NOT available, so use static analysis:

1. Read all Phase 36 PLANs and SUMMARYs to extract must_haves
2. Read and verify Dockerfile:
   - INFRA-04: Multi-stage build producing minimal image -- verify FROM statements (builder stage + distroless/cc-debian12:nonroot), COPY --from=builder pattern, no unnecessary files in final image, TLS and SQLCipher compatibility (distroless cc variant for glibc)
   - Static checks: valid Dockerfile syntax, correct COPY paths, proper USER directive (nonroot), EXPOSE ports
   - Flag "Docker image build" as UNVERIFIED (requires Docker daemon) with note "Docker daemon not available on build machine"
3. Read and verify docker-compose.yml:
   - INFRA-05: Volume mounts for data/config/plugins, env injection for secrets, health check -- verify volumes, environment variables, healthcheck directive
   - Static validation: valid YAML structure, correct service definitions, port mappings
4. Read and verify deploy/blufio@.service (or similar):
   - INFRA-07: Multi-instance systemd template -- verify template unit syntax (uses %i or %I), per-instance config directories (/etc/blufio/instances/%i/), per-instance data directories (/var/lib/blufio/instances/%i/)
5. Verify healthcheck CLI subcommand exists in blufio crate
6. Run any available tests (e.g., `cargo test -p blufio` with healthcheck-related tests)
7. Write 36-VERIFICATION.md with full format

Per CONTEXT decision: "Build failure flagged as UNVERIFIED, doesn't block rest of verification." The Docker build itself should be UNVERIFIED due to missing Docker daemon. Static file analysis should be VERIFIED where possible.
  </action>
  <verify>
    <automated>test -f .planning/phases/36-docker-image-deployment/36-VERIFICATION.md && grep -c "VERIFIED\|UNVERIFIED" .planning/phases/36-docker-image-deployment/36-VERIFICATION.md</automated>
  </verify>
  <done>36-VERIFICATION.md exists with score X/3, Dockerfile and docker-compose.yml statically verified, Docker build flagged as UNVERIFIED with reason, systemd template verified</done>
</task>

</tasks>

<verification>
- Both VERIFICATION.md files exist with YAML frontmatter and score lines
- Combined coverage: 8 requirements (SKILL-01..05, INFRA-04, INFRA-05, INFRA-07) all have evidence
- `cargo test -p blufio-skill` passes including any new tests
- Docker verification notes are explicit about what was static vs live
</verification>

<success_criteria>
- 35-VERIFICATION.md has score X/5 with evidence for SKILL-01..05
- 36-VERIFICATION.md has score X/3 with evidence for INFRA-04, INFRA-05, INFRA-07
- Docker build status explicitly documented as UNVERIFIED due to missing daemon
- All 8 requirement IDs appear in verification evidence
</success_criteria>

<output>
After completion, create `.planning/phases/39-integration-verification/39-04-SUMMARY.md`
</output>
