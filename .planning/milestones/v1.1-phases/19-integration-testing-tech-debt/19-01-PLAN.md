---
phase: 19-integration-testing-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/blufio-prometheus/src/recording.rs
  - crates/blufio-config/src/model.rs
  - crates/blufio-mcp-server/src/transport.rs
  - crates/blufio-gateway/src/server.rs
autonomous: true
requirements: [INTG-04, INTG-05]

must_haves:
  truths:
    - "MCP Prometheus metrics (connection count, tool response sizes, context utilization) are registered and recorded"
    - "Connection count limits are enforced with configurable defaults"
    - "Over-limit MCP connections receive HTTP 503"
  artifacts:
    - path: "crates/blufio-prometheus/src/recording.rs"
      provides: "MCP metric registration and recording helpers"
      contains: "blufio_mcp_connections_total"
    - path: "crates/blufio-config/src/model.rs"
      provides: "max_connections config field"
      contains: "max_connections"
    - path: "crates/blufio-gateway/src/server.rs"
      provides: "Connection limit middleware on MCP router"
  key_links:
    - from: "crates/blufio-prometheus/src/recording.rs"
      to: "metrics-rs facade"
      via: "describe_counter!/describe_gauge!/describe_histogram! macros"
    - from: "crates/blufio-gateway/src/server.rs"
      to: "tower ConcurrencyLimit"
      via: "middleware layer on MCP router nest"
---

<objective>
Add MCP-specific Prometheus metrics and connection count limits.

Purpose: INTG-04 requires MCP observability (connection count, tool response sizes, context utilization). INTG-05 requires enforceable connection limits with configurable defaults.
Output: Extended recording.rs with MCP metrics, config field for max_connections, middleware wiring in server.rs.
</objective>

<execution_context>
@/Users/suman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/suman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@crates/blufio-prometheus/src/recording.rs
@crates/blufio-prometheus/src/lib.rs
@crates/blufio-config/src/model.rs
@crates/blufio-gateway/src/server.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MCP Prometheus metrics to recording.rs</name>
  <files>crates/blufio-prometheus/src/recording.rs, crates/blufio-prometheus/src/lib.rs</files>
  <behavior>
    - register_metrics() includes describe_counter! for blufio_mcp_connections_total
    - register_metrics() includes describe_gauge! for blufio_mcp_active_connections
    - register_metrics() includes describe_histogram! for blufio_mcp_tool_response_size_bytes
    - register_metrics() includes describe_gauge! for blufio_mcp_context_utilization_ratio
    - Helper functions record_mcp_connection(), set_mcp_active_connections(), record_mcp_tool_response_size(), set_mcp_context_utilization() exist and are pub
    - All new helpers are re-exported from lib.rs
  </behavior>
  <action>
    Extend `register_metrics()` in `crates/blufio-prometheus/src/recording.rs` with four new MCP metrics following the exact existing pattern:

    1. `blufio_mcp_connections_total` (counter) - "Total MCP connections by transport" with "transport" label
    2. `blufio_mcp_active_connections` (gauge) - "Currently active MCP connections"
    3. `blufio_mcp_tool_response_size_bytes` (histogram) - "MCP tool response sizes in bytes"
    4. `blufio_mcp_context_utilization_ratio` (gauge) - "Context window utilization ratio"

    Add recording helper functions following the existing pattern (e.g., `record_message`, `set_active_sessions`):
    - `record_mcp_connection(transport: &str)` - increments counter with transport label
    - `set_mcp_active_connections(count: f64)` - sets gauge
    - `record_mcp_tool_response_size(bytes: f64)` - records histogram
    - `set_mcp_context_utilization(ratio: f64)` - sets gauge

    Add pub use exports in `lib.rs` for all four new functions.

    NOTE: Do not add instrumentation call sites yet -- this task defines the metrics API. Call sites will be added by executors in future work as MCP code paths are exercised.
  </action>
  <verify>
    <automated>cargo build -p blufio-prometheus 2>&1 | tail -5</automated>
  </verify>
  <done>Four MCP metrics registered with describe_*! macros, four recording helper functions exported, crate compiles cleanly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add connection limit config and middleware</name>
  <files>crates/blufio-config/src/model.rs, crates/blufio-gateway/src/server.rs</files>
  <behavior>
    - McpConfig has a max_connections field with default of 10
    - TOML with mcp.max_connections = 5 parses correctly
    - MCP router in server.rs is wrapped with a concurrency limit layer
    - When max_connections is exceeded, additional requests receive 503
  </behavior>
  <action>
    1. In `crates/blufio-config/src/model.rs`, add `max_connections: usize` to the `McpConfig` struct with `#[serde(default = "default_mcp_max_connections")]`. Create the default function returning 10. This is conservative for a personal agent use case.

    2. In `crates/blufio-gateway/src/server.rs`, modify the MCP router nesting in `start_server()`. Before `app.nest("/mcp", mcp)`, wrap the MCP router with Tower's `ConcurrencyLimitLayer`:

    ```rust
    use tower::limit::ConcurrencyLimitLayer;

    if let Some(mcp) = mcp_router {
        let limited_mcp = mcp.layer(ConcurrencyLimitLayer::new(mcp_max_connections));
        app = app.nest("/mcp", limited_mcp);
    }
    ```

    The `mcp_max_connections` value needs to be passed into `start_server()`. Add it as a parameter or include it in `ServerConfig`. The `start_server()` function is called from `GatewayChannel`, which has access to the MCP config. Pass `config.mcp.max_connections` through.

    Check if `tower` already has `ConcurrencyLimitLayer` available through axum's re-exports or if you need to add `tower = { version = "0.4", features = ["limit"] }` to the gateway crate's Cargo.toml. If tower is already a transitive dependency, prefer using it directly.

    Over-limit behavior: Tower's ConcurrencyLimitLayer returns a 503 Service Unavailable when the limit is reached. This is the correct behavior -- no queuing needed.
  </action>
  <verify>
    <automated>cargo build -p blufio-gateway 2>&1 | tail -5</automated>
  </verify>
  <done>McpConfig has max_connections field defaulting to 10, MCP router wrapped with ConcurrencyLimitLayer, over-limit requests get 503, full workspace compiles</done>
</task>

</tasks>

<verification>
- `cargo build -p blufio-prometheus` succeeds
- `cargo build -p blufio-gateway` succeeds
- `cargo test -p blufio-prometheus` passes
- `cargo test -p blufio-config` passes (TOML parsing with max_connections)
- grep confirms all four `blufio_mcp_*` metrics registered in recording.rs
</verification>

<success_criteria>
- Four MCP Prometheus metrics registered and exported: connections_total, active_connections, tool_response_size_bytes, context_utilization_ratio
- max_connections config field with default 10 added to McpConfig
- MCP router wrapped with ConcurrencyLimitLayer in server.rs
- Full workspace compiles and existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-integration-testing-tech-debt/19-01-SUMMARY.md`
</output>
