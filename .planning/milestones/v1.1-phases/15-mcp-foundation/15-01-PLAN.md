---
phase: 15-mcp-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/blufio-skill/Cargo.toml
  - crates/blufio-anthropic/Cargo.toml
  - crates/blufio/Cargo.toml
  - crates/blufio-skill/src/provider.rs
  - crates/blufio-anthropic/src/lib.rs
  - Cargo.lock
autonomous: true
requirements: [FOUND-03]

must_haves:
  truths:
    - reqwest 0.13 resolves as the single reqwest version in cargo tree
    - rmcp 0.17.0 is declared as a workspace dependency
    - schemars 1.0 is declared as a workspace dependency
    - cargo build succeeds with no reqwest version conflicts
  artifacts:
    - Cargo.toml with updated workspace dependencies
    - Cargo.lock with resolved dependency tree
  key_links:
    - workspace reqwest version matches rmcp's dependency
---

<objective>
Upgrade reqwest from 0.12 to 0.13 workspace-wide and add rmcp 0.17.0 + schemars 1.0 as workspace dependencies.

Purpose: rmcp 0.17.0 depends on reqwest 0.13.2. This plan resolves the version conflict before MCP crates can be scaffolded. It also adds rmcp and schemars to the workspace dependency table so MCP crates can reference them.

Output: Updated root Cargo.toml with reqwest 0.13, rmcp 0.17, schemars 1.0; all existing crates compile cleanly.
</objective>

<execution_context>
@/Users/suman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/suman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-mcp-foundation/15-RESEARCH.md

<interfaces>
<!-- Key dependency declarations the executor modifies -->

From Cargo.toml (workspace root):
```toml
[workspace.dependencies]
reqwest = { version = "0.12", features = ["json", "rustls-tls", "stream"], default-features = false }
# rmcp and schemars not yet present
```

From crates/blufio-skill/Cargo.toml:
```toml
[dependencies]
reqwest.workspace = true
```

From crates/blufio-anthropic/Cargo.toml:
```toml
[dependencies]
reqwest.workspace = true  # (verify this exists)
```

From crates/blufio/Cargo.toml:
```toml
[dependencies]
reqwest = { workspace = true }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade reqwest to 0.13 and add rmcp + schemars workspace deps</name>
  <files>Cargo.toml</files>
  <action>
In root Cargo.toml `[workspace.dependencies]`:

1. Change reqwest version from "0.12" to "0.13":
   ```toml
   reqwest = { version = "0.13", features = ["json", "rustls-tls", "stream"], default-features = false }
   ```

2. Add rmcp with default-features = false (individual crates will enable specific features):
   ```toml
   rmcp = { version = "0.17", default-features = false }
   ```

3. Add schemars:
   ```toml
   schemars = "1"
   ```

Do NOT change any other workspace dependencies. The rand version (0.8) stays -- rmcp brings its own rand 0.10 as an optional dep and they coexist fine.
  </action>
  <verify>
    <automated>grep -E 'reqwest.*"0\.13"|rmcp.*"0\.17"|schemars.*"1"' Cargo.toml | wc -l | grep -q 3</automated>
  </verify>
  <done>Root Cargo.toml declares reqwest 0.13, rmcp 0.17, schemars 1 as workspace dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Fix any reqwest 0.13 API breaks across the workspace and verify build</name>
  <files>crates/blufio-skill/src/provider.rs, crates/blufio-anthropic/src/lib.rs</files>
  <action>
Run `cargo build 2>&1` and fix any compilation errors from the reqwest 0.12 -> 0.13 upgrade:

1. Check for breaking changes in reqwest 0.13. Common ones:
   - `Client::new()` may require TLS backend feature selection
   - `Response` method signatures may have changed
   - Redirect policy API may have changed
   - Feature flag names may have changed (e.g., `rustls-tls` might now be `rustls-tls-native-roots` or similar)

2. Search for all reqwest usage in the workspace:
   - `crates/blufio-skill/src/provider.rs` - HTTP tool uses reqwest for outbound requests
   - `crates/blufio-anthropic/src/lib.rs` - Anthropic API client uses reqwest

3. For each usage, verify the API calls still compile. Fix any type mismatches, renamed methods, or changed signatures.

4. If reqwest 0.13 changed the `rustls-tls` feature name, update it in root Cargo.toml. Check with `cargo tree -i reqwest -f "{p} {f}"` to verify features resolve.

5. Run full workspace build: `cargo build --workspace`

6. Run tests: `cargo test --workspace`

Note: If the API is unchanged (just a version bump), this task may require zero code changes. Still verify by building.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -1 | grep -q "Finished"</automated>
  </verify>
  <done>All existing crates compile cleanly with reqwest 0.13. No regressions in cargo test.</done>
</task>

</tasks>

<verification>
- `cargo tree -i reqwest` shows only reqwest 0.13.x (no 0.12.x)
- `grep 'rmcp' Cargo.toml` shows rmcp 0.17 in workspace deps
- `grep 'schemars' Cargo.toml` shows schemars 1 in workspace deps
- `cargo build --workspace` succeeds
- `cargo test --workspace` passes
</verification>

<success_criteria>
- reqwest upgraded to 0.13 workspace-wide with no version conflicts
- rmcp 0.17.0 added as workspace dependency (default-features = false)
- schemars 1.0 added as workspace dependency
- All existing crates compile and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-mcp-foundation/15-01-SUMMARY.md`
</output>
